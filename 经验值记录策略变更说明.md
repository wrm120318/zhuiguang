# 经验值记录策略变更说明

## 变更概述

**v2.1.8 版本修改了删除美文时的经验值处理逻辑。**

## 变更前（v2.1.7及之前）

删除美文时：
1. 查询该美文相关的所有经验值记录
2. 计算总经验值变化
3. **添加一条负向记录**：`删除美文《XXX》回收经验`
4. 更新用户经验值

**示例：**
```
删除前：
- login: +5
- article（审核通过）: +15
- like: +1
- like_cancel: -1
总计：20

删除后：
- login: +5
- article（审核通过）: +15
- like: +1
- like_cancel: -1
- 删除回收: -20
总计：0
```

## 变更后（v2.1.8）

删除美文时：
1. 查询该美文相关的所有经验值记录
2. 计算总经验值变化
3. **直接删除相关记录**（article、like类型）
4. 更新用户经验值

**示例：**
```
删除前：
- login: +5
- article（审核通过）: +15
- like: +1
- like_cancel: -1
总计：20

删除后：
- login: +5
总计：5
```

## 变更原因

用户反馈：
> "回收的经验值不要再单独显示成记录啊、规则啊，它就是回收而已，原本的记录没有了"

用户希望：
- 删除美文后，相关的经验值记录（审核通过、点赞等）直接从列表中消失
- 不再显示"删除回收经验"这类负向记录
- 经验值记录只显示原始的操作记录

## 代码修改

### worker-api.ts (1442-1451)

**变更前：**
```typescript
// 删除前回收已发放的经验（美文发布/审核通过/点赞相关）
const expUid = Number(a.actual_user_id) || Number(a.user_id)
if (expUid && a.title) {
  const logs = await all<{ exp_change: number }>("SELECT exp_change FROM exp_logs WHERE user_id=? AND action_type IN ('article','like') AND description LIKE ?", expUid, `%${a.title}%`)
  const total = logs.reduce((s, l) => s + (l.exp_change || 0), 0)
  if (total) await addExp(expUid, -total, 'article', `删除美文《${a.title}》回收经验`)
}
```

**变更后：**
```typescript
// 删除前直接删除相关的经验值记录（美文审核通过/点赞相关）
const expUid = Number(a.actual_user_id) || Number(a.user_id)
if (expUid && a.title) {
  // 计算要回收的经验值（用于更新用户exp）
  const logs = await all<{ exp_change: number }>("SELECT exp_change FROM exp_logs WHERE user_id=? AND action_type IN ('article','like') AND description LIKE ?", expUid, `%${a.title}%`)
  const total = logs.reduce((s, l) => s + (l.exp_change || 0), 0)
  // 删除相关经验值记录
  await run("DELETE FROM exp_logs WHERE user_id=? AND action_type IN ('article','like') AND description LIKE ?", expUid, `%${a.title}%`)
  // 更新用户经验值
  if (total) await run('UPDATE users SET exp = MAX(0, exp - ?) WHERE id = ?', total, expUid)
}
```

### server/index.ts

同步修改，保持一致。

## 界面显示变化

### 经验值记录界面

**变更前：**
- 显示所有操作记录，包括"删除回收经验"这类负向记录
- 删除美文后，用户仍能看到之前点赞、审核通过等记录

**变更后：**
- 只显示原始操作记录（登录、审核通过、点赞等）
- 删除美文后，相关的经验值记录直接从列表中消失
- 不再显示"回收"、"删除"等负向记录

### 经验值说明界面

无变化，仍然显示所有经验值规则（包括删除类规则，值为0表示不配置）。

### 管理员设置界面

无变化，仍然只显示正向获取经验值的规则。

## 测试验证

### 测试步骤

1. 用户44（王瑞明）发布美文《追光》
2. 管理员审核通过，用户获得+15经验值
3. 用户点赞美文，获得+1经验值
4. 用户取消点赞，回收-1经验值
5. 用户删除美文《追光》
6. 检查经验值记录和用户状态

### 测试结果

**删除前：**
```
用户44 exp: 26
经验值记录：
- id=130, login, +5
- id=132, article, +15
- id=133, practice_pass, +5
- id=138-148, like/like_cancel, 0（相互抵消）
```

**删除后：**
```
用户44 exp: 5
经验值记录：
- id=130, login, +5
- id=133, practice_pass, +5
（其他记录已删除）
```

**结论：** ✓ 删除美文后，相关经验值记录直接从列表中消失，用户经验值正确减少。

## 部署状态

- ✓ 代码已推送到GitHub
- ✓ 前端将通过GitHub Actions自动部署到Cloudflare Pages
- ✓ 后端Worker需要通过Cloudflare Dashboard手动部署（本地环境有问题）

## 注意事项

1. **数据完整性**：直接删除经验值记录会丢失历史数据，但符合用户要求
2. **审计需求**：如果需要完整的审计日志，建议保留删除前的记录备份
3. **用户体验**：删除后记录直接消失，更简洁直观
