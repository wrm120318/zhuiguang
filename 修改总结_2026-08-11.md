# 经验值系统修改总结

## 修改内容

### 1. 前端修改
- `GuideView.vue`: 使用 excludeKeys 过滤删除/取消类规则
- `ExpRulesView.vue`: 移除删除规则配置项

### 2. 后端修改
- `server/index.ts`: 
  - 修改 getExpRules() API，过滤删除规则
  - 修改删除文章/资料/博客/查询逻辑，直接 DELETE exp_logs 记录
  
- `worker-api.ts`:
  - 修改 getExpRules() 函数，过滤删除规则
  - 修改删除文章/资料/博客/查询逻辑，直接 DELETE exp_logs 记录
  - 修改取消点赞逻辑，直接 DELETE 点赞时的 +1 记录

## 修改逻辑

**之前**: 删除/取消操作 → 添加负值记录到 exp_logs
**现在**: 删除/取消操作 → 直接 DELETE 原来的正值记录

## 副作用检查

✓ 只修改了经验值相关的逻辑
✓ 不影响其他功能（登录、注册、点赞、评论等）
✓ 删除操作仍然会正确回收经验值
✓ 用户经验值不会"倒扣"

## 部署状态
- 后端：已重新部署
- 前端：已重新构建并部署
