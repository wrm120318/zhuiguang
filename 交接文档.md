# 追光 · 学科共享平台 — 交接文档

> 版本：v4.1.2
> 创建时间：2026-08-15
> 最后更新：2026-08-28
> 项目主页：https://github.com/wrm120318/zhuiguang

---

## 一、项目身份卡

| 字段 | 值 |
|---|---|
| 项目中文名 | 追光 · 学科共享平台 |
| 项目英文名 | zhuiguang |
| 用户访问域名 | https://xkzg.de5.net |
| 后端 API 域名 | https://api.xkzg.dpdns.org |
| GitHub 仓库 | https://github.com/wrm120318/zhuiguang |
| 服务人群 | 中学教师（6-10 人）+ 学生（50-60 人，峰值并发约 65） |
| 默认超管账号 | `admin` / `admin123456` |
| 开源协议 | MIT |
| 当前版本 | v4.2.2（通用 Markdown 富文本编辑器 + 美文/公告可多次编辑 + 移动端适配 + 博客/论坛评论点赞漏通知修复） |

---

## 二、生产凭证与关键资源

### 2.1 云端服务

| 服务 | 账号/标识 | 用途 | 状态 |
|---|---|---|---|
| Cloudflare | 已有账号（绑定 `xkzg.dpdns.org` / `xkzg.de5.net`） | Pages 前端 + Workers 后端 + D1 数据库 | 运行中 |
| Cloudflare Workers | `zhuiguang-api` | 后端 API（Hono，154 路由） | 运行中 |
| Cloudflare D1 | `zhuiguang-db`（ID: `996ab327-1a44-47fb-ac1d-5ab963dd04a5`） | 数据库（24 张表） | 运行中 |
| Cloudflare Pages | `zhuiguang` 仓库自动构建 | 前端静态托管 | 运行中 |
| Supabase | Project: `njwkkinzgmwzyfifagwl` | 文件存储（Bucket: `zhuiguang`，Public） | 运行中 |
| GitHub | `wrm120318/zhuiguang` | 代码仓库 + Pages 自动部署触发 | 运行中 |

### 2.2 本地凭证（不入库）

| 文件 | 说明 |
|---|---|
| `.env` | 环境变量（JWT_SECRET、CLOUDFLARE_API_TOKEN、CLOUDFLARE_ACCOUNT_ID、SUPABASE_SERVICE_KEY 等） |
| `wrangler.toml` | Workers 配置文件（D1 binding + 环境变量） |

> **铁律**：以上文件禁止入库，`.gitignore` 已排除。

### 2.3 密钥获取路径

| 密钥 | 获取位置 |
|---|---|
| `CLOUDFLARE_API_TOKEN` | Cloudflare Dashboard → My Profile → API Tokens |
| `CLOUDFLARE_ACCOUNT_ID` | Cloudflare Dashboard → 任意 Zone → API → Account ID |
| `SUPABASE_SERVICE_KEY` | Supabase Dashboard → Settings → API → service_role（**不是** anon key） |
| `SUPABASE_URL` | Supabase Dashboard → Settings → API → Project URL |
| `SUPABASE_BUCKET` | Supabase Dashboard → Storage → Buckets → `zhuiguang` |
| `JWT_SECRET` | 自行生成随机字符串，建议 32 位以上 |

---

## 三、部署架构

### 3.1 当前架构（v2.0.0+）

```
用户浏览器 → https://xkzg.de5.net
        |
        v
Cloudflare Pages（Vue 3 SPA + Element Plus）
  - 构建命令：npm run build
  - 输出目录：dist/
  - 自定义域名：xkzg.de5.net
  - GitHub push 自动构建

        | API 同源请求（/api/xxx）
        v
Cloudflare Workers（worker-api.ts，Hono 框架）
  - 部署命令：npx wrangler deploy
  - 自定义域名：api.xkzg.dpdns.org
  - 154 路由

        |
        v
Cloudflare D1（zhuiguang-db）
  - 24 张表
  - 建表脚本：schema.sql
  - 执行命令：npx wrangler d1 execute --file=schema.sql

        |
        v
Backblaze B2 私有桶（zhuiguang-k12-004）
  - 所有上传统一经后端 Worker 代理：前端 POST /api/upload/image|file → Worker 写 B2，返回 /api/file/{id}
  - 不再使用 Supabase presign 直传（Supabase 已废弃，仅保留只读回退）
```

### 3.2 历史架构（已废弃）

| 版本 | 架构 | 状态 |
|---|---|---|
| v1.0.0 | Pinggy SSH 隧道 + 本地 Node.js Express + SQLite | 已废弃 |
| v1.1.0 | 同上 + Bug9 修复 | 已废弃 |
| v1.2.0 | Worker v5 双路兜底 + start-all 依赖加固 | 已废弃 |
| v2.0.0 | Cloudflare Workers + D1 + Pages + Supabase | 已上线 |

---

## 四、技术栈

| 层级 | 技术 | 说明 |
|---|---|---|
| 前端框架 | Vue 3 + Vue Router + Pinia | 组合式 API，状态管理 |
| UI 组件库 | Element Plus | 按需自动导入 |
| 构建工具 | Vite 5 | 生产构建输出至 dist/ |
| 图表 | ECharts 5 | 排行榜、统计图表 |
| Markdown | marked | 美文/指南富文本渲染 |
| Excel | xlsx | 成绩导入导出 |
| 后端框架 | Hono 4 | Cloudflare Workers 原生 |
| 后端运行时 | Cloudflare Workers | 无服务器，边缘计算 |
| 数据库 | Cloudflare D1 | SQLite 兼容，serverless |
| 文件存储 | Backblaze B2 私有桶 | 统一经 Worker 代理上传（前端直传已废弃） |
| 认证 | JWT + bcryptjs | Bearer Token，7 天过期 |

---

## 五、数据库结构

共 **24 张表**（已与生产 D1 核对一致），建表脚本见 `schema.sql`：

| # | 表名 | 用途 |
|---|---|---|
| 1 | users | 用户表（账号、密码哈希、角色、经验值、等级、状态） |
| 2 | classes | 班级表 |
| 3 | class_members | 班级成员表（学生/教师与班级、学科关联） |
| 4 | subjects | 学科表（名称、图标、颜色、模块配置、公告） |
| 5 | articles | 美文表（标题、内容、封面、图片、标签、状态、点赞、浏览） |
| 6 | resources | 资源表（文件名、类型、大小、路径、分类、下载量） |
| 7 | query_tasks | 查询任务表（成绩查询任务配置） |
| 8 | query_rows | 查询行表（成绩数据行） |
| 9 | exp_logs | 经验日志表（用户经验变动记录） |
| 10 | notices | 通知表（站内通知） |
| 11 | pages | 通用页面表（公告、指南、**论坛帖子（ptype='forum'）**） |
| 12 | page_comments | 页面评论表 |
| 13 | messages | 站内信表（私信、附件、已读状态） |
| 14 | quizzes | 题库表（测验配置） |
| 15 | quiz_questions | 题目表（题型、内容、选项、答案、分值） |
| 16 | quiz_submissions | 提交记录表（答卷、得分、批改状态） |
| 17 | subject_questions | 学科题目池（单题训练用） |
| 18 | practice_submissions | 单题训练提交记录 |
| 19 | likes_map | 点赞表（用户-目标类型-目标 ID 唯一） |
| 20 | article_comments | 美文评论表 |
| 21 | feature_flags | 功能开关表（KV 结构，如注册开关） |
| 22 | themes | 主题表（配色配置） |
| 23 | settings | 全局设置表（KV 结构） |
| 24 | forum_topics | **【v4.1.0 新增】** 论坛话题表（学科论坛的话题/标签，按学科隔离） |

> ⚠️ **历史坑（v4.2.2 已修）**：`schema.sql` 曾长期停留在 23 张表，缺 `forum_topics` 表以及 `pages.subject_id/topic_ids`、`page_comments.parent_id`、`article_comments.parent_id`、`subjects.forum_auto_approve_threshold` 共 5 个字段。这些在生产 D1 上是通过历次**在线 ALTER** 补上的，但 schema.sql 没同步 → **新环境照 schema.sql 建库会少表少字段，论坛功能直接 500**。现已全部补入 schema.sql。

### 5.1 常用数据库操作

```bash
# 查询用户数
npx wrangler d1 execute zhuiguang-db --remote --command="SELECT COUNT(*) FROM users"

# 查看所有表
npx wrangler d1 execute zhuiguang-db --remote --command=".tables"

# 执行建表脚本
npx wrangler d1 execute zhuiguang-db --remote --file=schema.sql

# 执行任意 SQL
npx wrangler d1 execute zhuiguang-db --remote --command="SELECT * FROM users WHERE username='admin'"
```

### 5.2 在线 ALTER（v4.1.0 ~ v4.2.1 历次补字段/新表）

> **现状（v4.2.2 起）**：`schema.sql` 已内置全部 24 张表 + 全部字段，且所有 `CREATE TABLE` / `CREATE INDEX` 均为 `IF NOT EXISTS`，**幂等可重复执行**。
> - 新部署 D1：一次性跑 `schema.sql` 即可拿到全部 24 张表 + 全部字段。
> - 老 D1：字段已齐备，再跑 `schema.sql` 不会报错，也不会重复建表。
>
> 若怀疑某个老 D1 缺表/缺字段，先用 5.3 自检确认，再按下面命令逐条补（`ALTER TABLE ... ADD COLUMN` 在字段已存在时会报错，属正常，忽略即可）：

```bash
# v4.2.1：通知点击跳转锚点
npx wrangler d1 execute zhuiguang-db --remote --command="ALTER TABLE notices ADD COLUMN target_url TEXT"

# v4.2.0：评论二级回复（朋友圈折叠树）
npx wrangler d1 execute zhuiguang-db --remote --command="ALTER TABLE article_comments ADD COLUMN parent_id INTEGER"
npx wrangler d1 execute zhuiguang-db --remote --command="ALTER TABLE page_comments ADD COLUMN parent_id INTEGER"
npx wrangler d1 execute zhuiguang-db --remote --command="CREATE INDEX IF NOT EXISTS idx_art_c_p ON article_comments(parent_id)"
npx wrangler d1 execute zhuiguang-db --remote --command="CREATE INDEX IF NOT EXISTS idx_page_c_p ON page_comments(parent_id)"

# v4.1.0：学科论坛（pages 补字段 + forum_topics 新表）
npx wrangler d1 execute zhuiguang-db --remote --command="ALTER TABLE pages ADD COLUMN subject_id INTEGER DEFAULT NULL"
npx wrangler d1 execute zhuiguang-db --remote --command="ALTER TABLE pages ADD COLUMN topic_ids TEXT DEFAULT '[]'"
npx wrangler d1 execute zhuiguang-db --remote --command="CREATE TABLE IF NOT EXISTS forum_topics (id INTEGER PRIMARY KEY AUTOINCREMENT, subject_id INTEGER NOT NULL, name TEXT NOT NULL, color TEXT DEFAULT '#BA7517', created_by INTEGER, created_at TEXT DEFAULT (datetime('now','+8 hours')), FOREIGN KEY(subject_id) REFERENCES subjects(id), FOREIGN KEY(created_by) REFERENCES users(id))"
npx wrangler d1 execute zhuiguang-db --remote --command="CREATE INDEX IF NOT EXISTS idx_forum_topics_subject ON forum_topics(subject_id)"

# v4.1.1：论坛免审阈值（按学科）
npx wrangler d1 execute zhuiguang-db --remote --command="ALTER TABLE subjects ADD COLUMN forum_auto_approve_threshold INTEGER DEFAULT 0"
```

### 5.3 一致性自检（部署 / 接手时先跑一遍）

```bash
# 表数应为 24（另有 _cf_KV 为 D1 系统表，不算）
npx wrangler d1 execute zhuiguang-db --remote --command="SELECT name FROM sqlite_master WHERE type='table' AND name NOT LIKE '_cf_%' ORDER BY name"

# 路由数（生产 worker-api.ts）
grep -E "app\.(get|post|put|patch|delete)\(" worker-api.ts | wc -l

# 关键字段自检（缺哪个就按 5.2 补哪个）
npx wrangler d1 execute zhuiguang-db --remote --command="PRAGMA table_info(notices)"        # 需含 target_url
npx wrangler d1 execute zhuiguang-db --remote --command="PRAGMA table_info(article_comments)" # 需含 parent_id
npx wrangler d1 execute zhuiguang-db --remote --command="PRAGMA table_info(page_comments)"    # 需含 parent_id
npx wrangler d1 execute zhuiguang-db --remote --command="PRAGMA table_info(pages)"            # 需含 subject_id / topic_ids
npx wrangler d1 execute zhuiguang-db --remote --command="PRAGMA table_info(subjects)"         # 需含 forum_auto_approve_threshold

# forum_topics 表必须存在
npx wrangler d1 execute zhuiguang-db --remote --command="SELECT name FROM sqlite_master WHERE type='table' AND name='forum_topics'"
```

---

## 六、核心文件索引

### 6.1 生产后端

| 文件 | 说明 |
|---|---|
| `worker-api.ts` | 生产后端（Hono，**154 路由**），Cloudflare Workers 部署 |
| `wrangler.toml` | Workers 配置（D1 binding + 环境变量） |
| `schema.sql` | D1 建表脚本（**24 张表** + 索引，幂等） |

### 6.2 前端

| 文件/目录 | 说明 |
|---|---|
| `src/api/http.ts` | axios 实例，同源模式（`/api/xxx`） |
| `src/api/index.ts` | API 接口定义（23 个模块） |
| `src/store/theme.ts` | 主题状态管理 |
| `src/store/user.ts` | 用户状态管理 |
| `src/router/index.ts` | 路由配置（含权限守卫） |
| `src/styles/main.css` | 全局样式（经典 :root 像素级不动；`.zg-inkgold`/`.zg-inkgold-dark` 墨金双档 + 全站液态玻璃材质系统 v7 L1/L2/L3；`.zg-grad-text` 渐变文字；弹窗透明化；`:focus-visible` 已排除输入框类） |
| `index.html` | HTML 入口（含 `?v=` 版本戳） |
| `vite.config.ts` | Vite 配置（代理、自动导入） |
| `src/components/MarkdownEditor.vue` | **【v4.2.2 新增】** 通用 Markdown 富文本编辑器公共组件（工具栏 + 实时预览 + 拖拽上传 + 撤销重做 + 移动端适配） |
| `src/components/CommentTree.vue` | **【v4.2.0 新增】** 评论二级回复公共组件（美文/博客/论坛统一接入） |
| `src/views/ArticleEditView.vue` | 美文编辑（接入 MarkdownEditor，支持新建/编辑 + 代发作者下拉） |
| `src/views/AnnouncementEditView.vue` | 公告编辑（接入 MarkdownEditor，仅超管） |
| `src/views/BlogEditView.vue` | 博客编辑（接入 MarkdownEditor） |
| `src/views/SubjectForumEditView.vue` | 论坛帖子编辑（接入 MarkdownEditor + 草稿自动保存；编辑模式不加载草稿，始终显示原帖） |
| `src/views/admin/GuideEditView.vue` | 网站说明编辑（接入 MarkdownEditor） |
| `src/utils/marked-extensions.ts` | **【v4.2.2 新增】** 8 类 marked 扩展（KaTeX/@提及/高亮/图片尺寸/嵌入/file 附件/HTML 白名单） |
| `public/_headers` | Cloudflare Pages CDN 头部策略 |
| `public/_redirects` | SPA fallback（所有路径回 index.html） |

### 6.3 本地开发

| 文件/目录 | 说明 |
|---|---|
| `server/index.ts` | 本地 Express 后端（与 Worker 保持路由一致） |
| `server/db.ts` | 本地 SQLite 连接 |
| `server/local.db` | 本地数据库（不入库，.gitignore 排除） |

### 6.4 运维脚本

| 文件 | 说明 |
|---|---|
| `scripts/migrate-to-d1.sh` | SQLite → D1 数据迁移 |
| `scripts/backup-sqlite.sh` | 本地 SQLite 备份 |

---

## 七、文件上传架构

### 7.1 架构设计

- **所有文件**（图片 / 文档 / 头像 / 封面 / 附件）→ 统一经后端 Worker 代理：前端 `POST /api/upload/image`（图片）或 `/api/upload/file`（文档），Worker 内部写 B2 私有桶，返回 `/api/file/{id}` 直链。
- **不再**使用 Supabase presign 直传（v4.4.0 起废弃，Supabase 仅保留只读回退）。
- el-upload 组件**必须**使用 `:http-request` 自定义上传，**禁止**使用 `:before-upload`

### 7.2 涉及上传的页面

所有使用 `el-upload` 的页面（清单见 `src/views/` 中各 `.vue` 文件），改一个都不能漏：
- 美文编辑/发布
- 资料上传
- 文章编辑/发布
- 查询成绩导入

### 7.3 排查上传问题

1. 确认 B2 配置正确（`wrangler.toml` 的 `[vars]`：`B2_KEY_ID` / `B2_APPLICATION_KEY` / `B2_BUCKET_ID` / `B2_BUCKET_NAME`）
2. 确认上传接口返回 200 与 `{url,fileId}`（公开图片免登录直出，见 v4.4.3）
3. F12 Network 面板看 `/api/upload/image`、`/api/upload/file` 请求是否返回 200
4. 测试 1MB 以上大文件（不能只测小文件）

---

## 八、角色权限体系

项目采用三角色体系，角色名为固定字符串，**禁止修改**：

| 角色 | 角色名（代码常量） | 权限范围 |
|---|---|---|
| 超级管理员 | `SUPER_ADMIN` | 全部权限：用户管理、功能开关、经验规则、主题配置、所有学科与班级、审核 |
| 教师 | `TEACHER` | 本学科管理、发布美文/资料/题库/成绩，无用户管理/功能开关/经验设置/删用户权限 |
| 学生 | `STUDENT` | 发布美文（待审核）、上传资料（待审核）、答题、查询成绩、站内信、查看内容 |

### 权限实现

- 敏感接口统一使用 `requireRole('SUPER_ADMIN')` 中间件保护
- 前端菜单通过 computed 按角色过滤显示
- 教师访问用户管理等接口会返回 403

---

## 九、业务规则（铁律，禁止修改）

1. **经验值登录积分**：同一天同一用户登录 N 次，只计 1 次经验。禁止改成每次登录都给。
2. **站内信联系人**：普通学生也能看到全部活跃用户（除自己），可主动发消息给任何人。禁止限制为"只能给超管发"或"只能看聊过的人"。
3. **审核/可见性规则**：
   - 超管/教师上传 → 直接 `approved`，立刻显示
   - 学生上传 → 状态 `pending` 等待审核
   - 学生自己的列表必须能看到自己的 `pending`（"待审核"标签）
4. **审核通过后经验值加给原作者**，不是审核人（Bug1 修复，禁止改回）。
5. **角色名固定**：`SUPER_ADMIN` / `TEACHER` / `STUDENT` 三个角色名为固定字符串，禁止改名、禁止新增角色。
6. **上传统一经 Worker 代理**：所有文件上传走 `/api/upload/image` 或 `/api/upload/file`（前端 `el-upload` + `:http-request`），由 Worker 写入 B2 私有桶；禁止改回 Supabase presign 直传。
7. **el-upload 上传**：必须使用 `:http-request` 自定义上传，禁止使用 `:before-upload`。
8. **已修复 Bug 不得改回**：详见本章。
9. **schema.sql 必须与生产 D1 保持一致（v4.2.2 新增）**：
   - 新增表 / 新增字段时，**先**改 `schema.sql`（`CREATE TABLE`/`CREATE INDEX` 一律 `IF NOT EXISTS` + 版本注释），**再**去生产 D1 跑 `ALTER TABLE`
   - 跑完 `ALTER` 必须 `PRAGMA table_info(表名)` 验证字段真的加上
   - 发布前跑一遍「5.3 一致性自检」（表数应为 24、关键字段齐全）
   - ⚠️ 否则新环境照 `schema.sql` 建库会少表少字段 → 功能直接 500（v4.2.2 已踩过此坑：缺 `forum_topics` + 5 个字段）

---

## 十、已修复 Bug 清单（禁止改回）

| Bug ID | 描述 | 修复版本 | 涉及文件 |
|---|---|---|---|
| Bug1 | 审核通过后经验值加给原作者，不是审核人 | v1.1.0 | `server/index.ts`, `worker-api.ts` |
| Bug2 | 美文评论数不一致 | v1.1.0 | `worker-api.ts` |
| Bug3 | 单题训练统计正确率显示错误 | v2.1.0 | `worker-api.ts`, `src/views/quiz/PracticeStatsView.vue` |
| Bug4 | 成绩导出 Excel 乱码 | v2.1.0 | `worker-api.ts` |
| Bug5 | 教师创建学科时班级关联错误 | v2.1.0 | `worker-api.ts` |
| Bug6 | 用户列表分页数据不一致 | v2.1.0 | `worker-api.ts` |
| Bug7 | 经验值日志显示异常 | v2.1.0 | `worker-api.ts`, `src/views/admin/ExpLogsView.vue` |
| Bug8 | 班级成员查询遗漏教师 | v2.1.0 | `worker-api.ts` |
| Bug9 | 综合 Bug 修复（9 个大修复全部完成） | v1.1.0 | `worker-api.ts`, `server/index.ts`, `src/styles/main.css` |
| Bug10 | 删除美文时评论经验值未回收 | v2.1.12 | `worker-api.ts`, `server/index.ts` |
| Bug11 | 主题发布全局不生效 | v2.1.7 | `src/store/theme.ts`, `worker-api.ts` |
| Bug12 | 弹窗关闭按钮消失 | v2.1.18 | `src/styles/main.css` |
| Bug13 | 通知中心（el-drawer）发黄 | v2.1.18 | `src/styles/main.css` |
| Bug14 | 输入框金色直角矩形边框（`:focus-visible` outline 不跟随圆角） | v3.0.0 | `src/styles/main.css` |
| Bug15 | 弹窗 header/footer 灰色矩形条 | v3.0.0 | `src/styles/main.css` |
| Bug16 | Hero 顶部釉光像矩形边框 | v3.0.0 | `src/styles/main.css`, `src/views/HomeView.vue` |
| Bug17 | 墨金深档变量特异性不足致深底深字不可读 | v3.0.0 | `src/styles/main.css` |

> **铁律**：以上修复的防御逻辑不得删除、不得修改、不得"顺手优化"。

---

## 十一、Markdown 富文本编辑器使用规范（v4.2.2 新增）

### 12.1 公共组件
- **位置**：`src/components/MarkdownEditor.vue`（约 350 行）
- **依赖**：`marked@18.x`（已有）+ `katex@0.16.11`（新增，CDN 引入 CSS）
- **扩展定义**：`src/utils/marked-extensions.ts`（约 230 行，8 类扩展）

### 12.2 支持的语法
| 语法 | 效果 |
|---|---|
| `**粗体**` / `*斜体*` / `~~删除线~~` | 文本格式 |
| `# H1` ~ `#### H4` | 标题分级 |
| `> 引用` | 引用块（左边竖线 + 背景色） |
| `- 列表` / `1. 列表` / `- [ ] 待办` | 列表 |
| `---` | 分割线（渐变） |
| `` `代码` `` / ` ```代码块``` ` | 行内代码 / 代码块（深色背景） |
| `\|表格\|` | 表格 |
| `==高亮==` | 黄色荧光笔效果（自定义扩展） |
| `$E=mc^2$` / `$$\int...$$` | KaTeX 数学公式（行内/块级） |
| `@[name](/user/uid)` | 用户提及（蓝色色块链接） |
| `![alt](url =100x100)` | 自定义尺寸图片 |
| `@\`https://...a.mp4\`` | 视频嵌入（HTML5 video） |
| `@[bilibili](BVxxx)` | B 站嵌入（iframe） |
| `@\`https://...a.pdf\`` | PDF 嵌入（iframe + 标题栏） |
| `file://文件名` | 附件引用（蓝色链接，题目/比赛/训练推荐） |
| `<details>/<summary>/<kbd>/<mark>/<sub>/<sup>/<ins>/<del>` | HTML 白名单子集 |

### 12.3 已接入 MarkdownEditor 的页面（必读，**禁止自行重新拼 textarea**）
| 页面 | 路由 |
|---|---|
| 美文编辑 | `/article/new` / `/article/:id/edit` |
| 公告编辑 | `/announcements/new` / `/announcements/:id/edit` |
| 博客编辑 | `/blog/new` / `/blog/:id/edit` |
| 论坛帖子编辑 | `/subject/:slug/forum/new` / `/subject/:slug/forum/post/:id/edit` |
| 网站说明编辑 | `/admin/guide` |

### 12.4 渲染入口
- **`src/utils/markdown.ts`** 暴露 `renderMarkdown(src)`：所有详情页（美文 / 公告 / 博客 / 论坛 / 网站说明）都用它渲染
- **`src/utils/marked-extensions.ts`** 暴露 `extractMentions(src)`：从 Markdown 源文中提取 `@[name](/user/uid)` 提及的用户列表（用于通知触发）
- **HTML 透传**：marked 默认行为，HTML 标签原样输出（已被白名单过滤）
- **老数据兼容**：contenteditable 时代的老 HTML 字符串，marked 透传后样式保留

### 12.5 写作 API（前端）
```typescript
import MarkdownEditor from '@/components/MarkdownEditor.vue'

<MarkdownEditor
  v-model="content"
  placeholder="支持 Markdown + HTML 子集 + KaTeX + 视频/PDF 嵌入..."
  :min-height="360"
  :enable-uploads="true"
  :enable-emoji="true"
  :enable-html="true"
/>
```

### 12.6 扩展编辑器的铁律
1. **所有富文本编辑场景必须用 `<MarkdownEditor>`**，禁止再写 textarea + insertTag 拼字符串
2. **所有富文本渲染场景必须用 `renderMarkdown()`**，禁止直接 `v-html`
3. **新增扩展语法**：在 `src/utils/marked-extensions.ts` 追加，统一注册
4. **KaTeX 升级**：在 `src/utils/marked-extensions.ts` 顶部 `katex.renderToString` 改配置，**不要**改 CDN 链接

---

## 十二、版本演进记录

| 版本 | 日期 | 主要内容 |
|---|---|---|
| v4.2.2 | 2026-08-29 | **通用 Markdown 富文本编辑器**（MarkdownEditor.vue 公共组件，支持 CommonMark + GFM + KaTeX 公式 + 用户提及 + ==高亮== + 视频/PDF/B站/file:// 嵌入 + HTML 子集；marked-extensions.ts 8 类扩展）；**美文可多次编辑**（PATCH /api/articles/:id，发布者/实际作者/超管/学科教师可编辑）；**公告可多次编辑**（复用 updatePage，超管可编辑）；同时支持 HTML 和 Markdown 粘贴；5 个编辑视图（美文/公告/博客/论坛/网站说明）全部接入 MarkdownEditor；ArticleEditView 新增代发作者下拉；markdown-body 渲染样式 207 行 |
| v4.2.1 | 2026-08-28 | **通知中心**：评论/点赞自动入通知中心 + 桌面右上角 ElNotification 弹窗（蓝色=评论、绿色=点赞、15s 轮询 + 增量检测 lastSeenIds 防历史通知轰炸） + 点击通知跳转 target_url（精确到 #comment-123 锚点 + smooth scroll） |
| v4.2.0 | 2026-08-28 | 评论二级回复（朋友圈折叠树）+ 抽公共 CommentTree 组件（美文/博客/论坛统一接入） |
| v4.1.6 | 2026-08-28 | 个人主页经验值进度条 bug（每级档位 60 写错为 100 导致 Lv≥2 全满条）→ 改用 utils/helpers.ts 的 expProgress |
| v4.1.5 | 2026-08-28 | 经验值说明页「发布论坛帖」漏加中文文案 bug 修复（用户面 ExpDocView + GuideView 两处 RULE_DESC 补齐） |
| v4.1.4 | 2026-08-28 | 论坛侧栏：最新→最热（views×0.4 + comments×0.6 加权 + 奖牌 Top3 + 副标显示浏览/评论数） |
| v4.1.3 | 2026-08-28 | 论坛 bug 二次修复：v-if 改 v-show 彻底根治预览后空白 + 免审阈值 UI 内嵌到论坛模块卡（论坛关闭时 input disabled） |
| v4.1.2 | 2026-08-28 | 论坛体验补完：发帖独立页（/forum/new + /forum/post/:id/edit）+ 草稿自动保存（useAutoSave）+ 桌面两栏中央阅读 + 修复预览后编辑器空白 |
| v4.1.1 | 2026-08-28 | 论坛体验补完：模块显隐开关 / 免审阈值（按学科，0~10000 字）/ 复用美文审核 UI 流程（AuditView 加「论坛」tab，帖子列表删掉快捷审核按钮）/ 富文本发帖（与博客一致工具栏） |
| v4.1.0 | 2026-08-27 | 学科论坛板块上线：每个学科独立的论坛板块，复用 pages 表 + 新增 forum_topics 表；11 个 API + 2 个新页面 + 3 个路由；话题标签由本学科教师/超管管理 |
| v3.0.0 | 2026-08-22 | 墨金学术双主题上线 + 全站液态玻璃 v7（L1/L2/L3）+ 自托管衬线字体 + 主题背景图 + 输入框/弹窗/Hero/深浅档 Bug 修复 |
| v3.0.10 | 2026-08-25 | 通知中心 direction 枚举值修复（rtl/ltr 替代 right/left）+ drawer 全局 position 强制 |
| v4.0.0 | 2026-08-26 | 15+ 业务 bug 集中修复：canManageSubject 异步化 + requireSubjectStaff 中间件 + 考试题源兼容模式 + 学科榜从 exp_logs 聚合 + 博客 PATCH/编辑入口 + 通知 read-all |
| v4.1.0 | 2026-08-27 | 学科论坛（复用 pages 表 + 新表 forum_topics） |
| v4.1.1~v4.1.6 | 2026-08-28 | 论坛体验补完 + 论坛审核 tab + 经验值 bug 修复 + 论坛免审阈值 + 富文本发帖 |
| v4.2.0 | 2026-08-28 | 评论二级回复（朋友圈折叠树） + 抽公共 CommentTree 组件 |
| **v4.2.1** | **2026-08-28** | **通知中心：comment / like 通知触发 + target_url 跳转 + 锚点定位** |
| v2.1.19 | 2026-08-16 | 文档全面完善 + 域名统一 + 清理液态玻璃残留代码 |
| v2.1.18 | 2026-08-14 | 弹窗关闭按钮统一修复 + 通知中心发黄修复 |
| v2.1.17 | 2026-08-14 | 所有弹窗 × 关闭按钮统一风格 |
| v2.1.16 | 2026-08-14 | 弹窗样式全面重构为 macOS 风格 |
| v2.1.15 | 2026-08-14 | 弹窗高斯模糊效果增强 |
| v2.1.14 | 2026-08-14 | Cloudflare 构建失败修复 + Element Plus 弹窗居中修复 |
| v2.1.13 | 2026-08-13 | Markdown 空格保留支持 |
| v2.1.12 | 2026-08-12 | Markdown 编辑器支持 + 删除美文评论经验值回收修复 |
| v2.1.11 | 2026-08-11 | 经验值说明页面修复 |
| v2.1.10 | 2026-08-11 | 删除经验值说明界面回收规则显示 |
| v2.1.9 | 2026-08-11 | 删除美文时经验值处理逻辑变更（直接删除，不写负向记录） |
| v2.1.8 | 2026-08-11 | 删除美文时经验值处理逻辑变更 |
| v2.1.7 | 2026-08-10 | 主题发布全局生效修复 |
| v2.1.6 | 2026-08-10 | 经验值系统测试验证 |
| v2.1.5 | 2026-08-10 | 单题训练教师批改模式补全 |
| v2.1.0 | 2026-08-10 | 单题训练按题维度统计 + 训练记录管理 |
| v2.0.0 | 2026-08-06 | 迁移至 Cloudflare Workers + D1 + Pages + Supabase 无服务器架构 |
| v1.2.0 | 2026-08-05 | Worker v5 双路兜底 + start-all 依赖加固 |
| v1.1.0 | 2026-08-05 | Bug9 大修复 + 经验值系统完善 |
| v1.0.0 | 2026-08-04 | Pinggy SSH 隧道 + 本地 Node.js Express + SQLite |

---

## 十三、运维指南

### 12.1 部署流程

```bash
# 1. 构建前端
npm run build

# 2. 推送前端（GitHub → Cloudflare Pages 自动构建）
git add -A && git commit -m "描述" && git push origin main

# 3. 部署后端 Worker（需手动执行）
source .env && npx wrangler deploy
```

### 12.2 数据库操作

```bash
# 建表
npx wrangler d1 execute zhuiguang-db --remote --file=schema.sql

# 查询
npx wrangler d1 execute zhuiguang-db --remote --command="SELECT COUNT(*) FROM users"

# 迁移数据（SQLite → D1）
bash scripts/migrate-to-d1.sh

# 备份
bash scripts/backup-sqlite.sh
```

### 12.3 发布前必做

1. `npm run build` 确认无错误
2. 三角色测试（admin/教师/学生各操作一遍）
3. 大文件上传测试（>1MB）
4. 访问 https://xkzg.de5.net 验证

---

## 十四、故障排查

| 现象 | 可能原因 | 解决方法 |
|---|---|---|
| 完全白屏 | 前端构建未部署 / CDN 缓存 | 等 1-2 分钟构建完成，强刷浏览器（Ctrl+Shift+R） |
| 页面打开但功能点不动 | 后端 Worker 未部署 | 确认 `worker-api.ts` 已 `npx wrangler deploy` |
| 接口返回 401 | JWT Token 过期（7 天有效） | 重新登录即可 |
| 上传无反应 | 误用了 `:before-upload` | 改回 `:http-request` |
| 大文件上传失败 | B2 配置错误 | 检查 `wrangler.toml` 的 `[vars]` 中 B2 四项是否填写 |
| 改了代码用户看不到 | 浏览器缓存 | 强刷浏览器，确认构建已部署 |
| 接口返回 403 | 权限不足 | 确认角色权限，教师不能访问用户管理 |
| 图片上传后裂图 / 打不开 | 公开图片需免登录直出 | v4.4.3 已修复（公开文件免登录）；若仍裂图硬刷清 CDN 缓存 |

---

## 十五、重要约束

1. **全免费、不绑卡**：所有云服务必须使用免费额度。
2. **"它能跑"就是最高标准**：项目已跑通、能用，所有坑都踩过、所有 Bug 都修过。维护者定位是维护者而非重写者。
3. **禁止随意更换技术栈**：禁止换部署平台、换数据库、换存储服务、禁止升级依赖版本。
4. **禁止重构、重写架构**：不要为技术优雅引入新 Bug。
5. **先读文档再碰代码**：改代码前必须先读本交接文档，重点是第 7 章（文件上传）、第 10 章（已修复 Bug）、第 九章（业务规则铁律）。
6. **发布前必做测试**：三角色测试 + 大文件上传测试 + 生产验证（铁律）。
7. **每次发版前必须备份**。

---

## 十六、相关文档索引

| 文档 | 说明 |
|---|---|
| `README.md` | 项目总览、快速开始、架构、部署步骤 |
| `CHANGELOG.md` | 版本更新日志 |
| `CONTRIBUTING.md` | 开发规范、铁律、测试要求 |
| `FAQ.md` | 高频问题问答库 |
| `DEPLOY_CHECKLIST.md` | 部署检查清单 |
| `工作日志_追光学科共享平台.md` | 开发运维工作日志 |
| `开发工作日志_追光平台.md` | 逐条开发操作记录 |
| `AI维护者提示词.md` | **【新维护者首读】** AI 接手必贴提示词（v3.0.0，含墨金双主题/液态玻璃17条铁律） |
| `docs/inkgold-redesign-v6.md` | 墨金全站柔光玻璃重设计 v6 设计稿（v7 落地前的设计依据） |
| `docs/inkgold-texture-upgrade.md` | 墨金质感升级设计稿（柔光玻璃 + 主题背景） |
| `docs/archive/` | 历史审计/对话/设计报告归档（4.0 审计报告、各版界面改进报告、首页重设计报告等备用） |

---

> 本文档是项目维护的权威参考。新维护者必读第 7、9、10 章，避免踩坑。
