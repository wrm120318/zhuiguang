# 追光 · 学科共享平台 — 完整交接文档

> 最后更新：2026-08-10 | 文档版本：v2.1.7 | 交接范围：开发+运维+部署+业务全量
> 配套日志：请同步阅读 `/workspace/开发工作日志_追光平台.md`（本轮起逐条记录所有操作）
> 配套AI提示词：`/workspace/给新AI维护者的提示词_首条消息必贴.txt`（开新会话第一条就贴这个）
> 配套GitHub文档：`/workspace/README.md`、`/workspace/CONTRIBUTING.md`、`/workspace/CHANGELOG.md`、`/workspace/FAQ.md`
>
> 【v2.0 重大变更】项目已从 Pinggy SSH隧道 + 本地Node.js Express 架构迁移至 Cloudflare Workers + D1数据库 + Pages 无服务器架构。旧的隧道架构相关内容保留为历史参考（标注【历史架构】），当前生产环境以 Cloudflare 架构为准。
>
> ## 版本历史

### v2.1.10 (2026-08-11)
- **修复经验值说明页面显示删除规则的问题**
  - 原因：之前修改了错误的文件（ExpDocView.vue），实际页面使用的是 GuideView.vue
  - 修改：`src/views/GuideView.vue`, `src/views/admin/ExpRulesView.vue`
  - 删除规则：`article_delete`, `resource_delete`, `blog_delete`, `query_delete`, `comment_delete`, `like_cancel`, `favorite_cancel`

### v2.1.9 (2026-08-11)
- **删除经验值说明和管理员设置中的回收规则显示**
  - 原因：回收规则是"原来给多少，现在收多少"，写有什么意义
  - 修改：`src/views/ExpDocView.vue`（错误文件）, `src/views/admin/ExpRulesView.vue`
  - 删除规则：`article_delete`, `resource_delete`, `blog_delete`, `query_delete`, `comment_delete`, `like_cancel`, `favorite_cancel`

### v2.1.8 (2026-08-11)
- **经验值记录策略修改**：删除美文时直接删除相关经验值记录，不再添加负向"回收"记录
  - 修改：`worker-api.ts`, `server/index.ts`
  - 效果：经验值记录中只显示原始操作记录，删除后记录直接消失

### v2.1.7 (2026-08-10)
- **BUG-28 修复**：删除美文时点赞数不一致问题
  - 问题：删除美文时未修正 `likes` 字段，导致数据不一致
  - 修复：删除前先查询点赞数并修正 `likes` 计数
  - 文件：`worker-api.ts`, `server/index.ts`

### v2.1.6 (2026-08-10)
- **经验值系统测试验证**
  - 点赞功能测试：用户点赞美文后经验值正确增加（+1），经验值记录正确写入
  - 取消点赞测试：用户取消点赞后经验值正确回收（-1），经验值记录正确写入
  - 删除美文测试：删除美文后相关经验值正确回收（-21），经验值记录正确写入
  - 经验值记录策略：删除时保留原有记录，新增负向回收记录，确保历史完整性
>
> ### v2.1.7 (2026-08-10)
> - **BUG-28 修复**：删除美文时点赞数不一致问题
>   - 问题：删除美文时未修正 `likes` 字段，导致数据不一致
>   - 修复：删除前先查询点赞数并修正 `likes` 计数
>   - 文件：`worker-api.ts`, `server/index.ts`
>
> ### v2.1.6 (2026-08-10)
> - **经验值系统测试验证**
>   - 点赞功能测试：用户点赞美文后经验值正确增加（+1），经验值记录正确写入
>   - 取消点赞测试：用户取消点赞后经验值正确回收（-1），经验值记录正确写入
>   - 删除美文测试：删除美文后相关经验值正确回收（-21），经验值记录正确写入
>   - 经验值记录策略：删除时保留原有记录，新增负向回收记录，确保历史完整性
>
> ### v2.1.5 (2026-08-10)
> - **BUG-27 修复**：Admin 经验设置默认值符号错误
>   - 问题：`ExpRulesView.vue` 中删除类行为均为正数，与后端不一致
>   - 修复：全部改为负数
>   - 文件：`src/views/admin/ExpRulesView.vue`

---

## 目录

1. [项目身份卡（第一优先级）](#1-项目身份卡第一优先级)
2. [项目负责人 & 凭证（最敏感信息）](#2-项目负责人--凭证最敏感信息)
3. [整体部署架构（含访问链路）](#3-整体部署架构含访问链路)
4. [技术栈 & 依赖版本（禁止随意更换）](#4-技术栈--依赖版本禁止随意更换)
5. [数据库结构（禁止随意ALTER表）](#5-数据库结构禁止随意alter表)
6. [核心文件索引（改哪件事看哪份文件）](#6-核心文件索引改哪件事看哪份文件)
7. [文件上传架构（最重要的设计，90%的上传Bug都和它有关）](#7-文件上传架构最重要的设计90的上传bug都和它有关)
8. [角色与权限体系](#8-角色与权限体系)
9. [核心业务规则（业务逻辑红线，不能改）](#9-核心业务规则业务逻辑红线不能改)
10. [所有已修复Bug清单（新AI不得改回去）](#10-所有已修复bug清单新ai不得改回去)
11. [所有已完成功能清单](#11-所有已完成功能清单)
12. [运维与部署指南（服务器/Cloudflare）](#12-运维与部署指南服务器cloudflare)
13. [常用命令速查](#13-常用命令速查)
14. [故障排查手册（遇到1016/白屏/上传失败怎么办）](#14-故障排查手册遇到1016白屏上传失败怎么办)
15. [给新维护者的严格约束（必须遵守）](#15-给新维护者的严格约束必须遵守)
16. [项目发展时间线（构建到现在的完整历史）](#16-项目发展时间线构建到现在的完整历史)
17. [Cloudflare Workers + D1 迁移架构（v2.0 当前生产架构）](#17-cloudflare-workers--d1-迁移架构v20-当前生产架构)
18. [管理后台运行监控（Cloudflare适配版）](#18-管理后台运行监控cloudflare适配版)
19. [全局索引表（自定义名词/文件/模块速查）](#19-全局索引表自定义名词文件模块速查)

---

## 1. 项目身份卡（第一优先级）

| 字段 | 值 |
|---|---|
| 项目中文名 | 追光 · 学科共享平台 |
| 项目英文名 | zhuiguang |
| 用户访问域名 | https://xkzg.dpdns.org |
| 后端API域名 | https://api.xkzg.dpdns.org（Cloudflare Workers 自定义域名） |
| 项目类型 | 中学校园师生学科学习共享站 |
| 服务人群 | 中学教师（6-10人）+ 学生（50-60人，峰值并发65） |
| 代码仓库（GitHub） | https://github.com/wrm120318/zhuiguang |
| 代码仓库（Gitcode） | （备份镜像，可选） |
| 本地项目根目录 | `/workspace` |
| 当前运行环境 | Cloudflare Workers（后端API）+ Cloudflare Pages（前端）+ Cloudflare D1（数据库）+ Supabase Storage（文件存储） |
| 【历史架构】运行环境 | 本机Linux容器 + Node.js 24 + Pinggy SSH隧道（已废弃，仅保留代码和脚本） |
| 当前对外访问方式 | Cloudflare Pages 直接服务前端静态资源 + Cloudflare Workers 提供 API |
| 【历史架构】对外访问方式 | Pinggy免费SSH隧道 + Cloudflare域名+Worker反向代理（已废弃） |
| 默认超管账号 | `admin` / `admin123456`（首次创建，可修改密码） |
| 默认初始数据 | 2个班级（高二1班、高二2班）+ 9个学科（语数英物化生政史地+信息技术） |
| D1数据库名称 | zhuiguang-db |
| D1数据库ID | 996ab327-1a44-47fb-ac1d-5ab963dd04a5 |
| Workers项目名 | zhuiguang-api |
| Pages项目名 | zhuiguang-pages（或直接绑定xkzg.dpdns.org域名） |

---

## 2. 项目负责人 & 凭证（最敏感信息）

> ⚠️ **绝密**：仅项目负责人和维护者可见，**绝对不要提交到GitHub，不要粘贴在公开地方！**

### 2.1 项目负责人联系信息

| 项 | 值 |
|---|---|
| 项目负责人 GitHub 账号 | `wrm120318` |
| 负责人登录邮箱 | wangruiming.0318@qq.com |
| GitHub 仓库授权方式 | 个人仓库（Public公开）+ 写入通过 GITHUB_TOKEN |

### 2.2 本地环境变量（.env 文件 /workspace/.env）

```bash
# ===== 追光 · 生产环境配置（当前服务器直接部署） =====

# JWT 密钥（随机生成，修改后所有已登录用户掉线）
# ⚠️ 真实值见服务器本地 /workspace/.env，不入库不进文档
JWT_SECRET=<见本地 /workspace/.env>
JWT_EXPIRES=7d

# 监听端口（所有脚本写死了3001，不要改）
PORT=3001

# 数据库：本地 SQLite（数据存在 server/local.db，不是远程 Turso）
TURSO_DATABASE_URL=file:./server/local.db
TURSO_AUTH_TOKEN=

# 文件存储：Supabase Storage（前端直传，后端生成 presign URL）
SUPABASE_URL=https://njwkkinzgmwzyfifagwl.supabase.co
# ⚠️ 真实值见服务器本地 /workspace/.env（service_role 密钥，不入库不进文档）
SUPABASE_SERVICE_KEY=<见本地 /workspace/.env>
SUPABASE_BUCKET=zhuiguang

# 服务器访问外网必须走的 HTTP 代理（删了 Turso/Supabase/GitHub 全部超时）
HTTPS_PROXY=http://127.0.0.1:18080

# GitHub Token（隧道守护脚本用于自动更新隧道URL）
# ⚠️ 真实值见服务器本地 /workspace/.env，不入库不进文档
GITHUB_TOKEN=<见本地 /workspace/.env>

# ===== Cloudflare 部署凭证（wrangler deploy 用） =====
# ⚠️ 真实值见服务器本地 /workspace/.env，不入库不进文档
CLOUDFLARE_API_TOKEN=<见本地 /workspace/.env>
CLOUDFLARE_ACCOUNT_ID=<见本地 /workspace/.env>
```

### 2.3 Supabase 后台

| 项 | 值 |
|---|---|
| 后台登录 | https://supabase.com（用负责人邮箱登录） |
| Project | `njwkkinzgmwzyfifagwl` |
| Region | （默认） |
| Bucket | `zhuiguang`（权限：public，Service Key写入） |
| Service Role Key | 见 .env SUPABASE_SERVICE_KEY，**是service_role不是anon_key！** |

### 2.4 Cloudflare 后台

| 项 | 值 |
|---|---|
| 注册邮箱 | wangruiming.0318@qq.com |
| 域名 | `xkzg.dpdns.org`（已托管在Cloudflare DNS） |
| Worker名称 | `zhuiguang-proxy` |
| Worker绑定路由 | `xkzg.dpdns.org/*` → Worker 反向代理 |
| Worker代码源 | GitHub仓库 `worker.js` 文件（需手动粘贴部署，无CI） |

### 2.5 Cloudflared Tunnel（已废弃，仅存档）

| 项 | 值 |
|---|---|
| Tunnel ID | 1389bb0a-73ea-4ea7-8304-d46e75f3b310 |
| Token | `<见服务器本地存档，已废弃不入库>` |
| 状态 | 废弃原因：当前服务器代理无法支持cloudflared的HTTP/2长连接，会超时 |
| Tunnel Token（cfut） | `<见服务器本地存档，已废弃不入库>` |
| 状态2 | 也无法通过代理连接，暂时不用 |

### 2.6 Turso 数据库（已废弃，仅存档）

| 项 | 值 |
|---|---|
| 原URL | libsql://zhuiguang-wrm2025.aws-ap-northeast-1.turso.io |
| 原Token | `<见服务器本地存档，已废弃不入库>` |
| 废弃原因 | 用户授权Turso时遇到clerk.turso.tech跳转验证无法操作，改用本地SQLite file:./server/local.db |

---

## 3. 整体部署架构（含访问链路）

### 3.0 当前生产架构（v2.0 Cloudflare Workers + D1）

```
用户浏览器
  ↓ 输入 https://xkzg.dpdns.org
Cloudflare Pages（前端静态资源托管）
  ↓ 返回 dist/ 下的 Vue SPA（index.html + JS + CSS）
  ↓ 前端 JS 通过 VITE_API_BASE_URL 调用后端 API
Cloudflare Workers（后端 API：zhuiguang-api）
  ↓ worker-api.ts（Hono框架，115+路由）
  ↓ 通过 D1 binding 访问数据库
Cloudflare D1 数据库（zhuiguang-db）
  ↓ 19张表，数据从本地SQLite迁移而来
  ↓ 文件上传/下载
Supabase Storage（zhuiguang bucket）
  ← 前端直传 presign URL（绕过后端）
```

**新架构核心优势**：
- 全球 CDN 秒开，无需隧道，永不掉线
- 免费额度：Workers 10万请求/天（实际用1-2万），D1 500万行读/天，Pages 无限请求
- 无需维护隧道守护脚本、keep-alive、never-die-guard 等保活机制
- 数据库从本地 SQLite 迁移到 Cloudflare D1，数据在云端持久化

### 3.1 【历史架构】Pinggy SSH隧道访问链路（已废弃，仅作参考）

> 以下为 v1.x 版本的旧架构，项目已迁移至 Cloudflare 无服务器架构。旧架构相关的脚本和代码仍保留在仓库中，但不再用于生产环境。

```
用户浏览器
  ↓ 输入 https://xkzg.dpdns.org
Cloudflare DNS（xkzg.dpdns.org托管在Cloudflare）
  ↓ 命中路由规则，转发给 Cloudflare Worker：zhuiguang-proxy
Cloudflare Worker v4（/workspace/worker.js）
  ↓ Worker每5秒从GitHub读取最新隧道URL
GitHub Raw：raw.githubusercontent.com/wrm120318/zhuiguang/main/tunnel-url.txt
  ↓ 拿到临时隧道域名（例：https://xxx-yyy.free.pinggy.net）
Pinggy SSH 免费隧道
  ↓ SSH反向隧道（每55分钟主动重连一次，避免60分钟过期）
本服务器内网端口 127.0.0.1:3001
  ↓ Node.js Express + tsx server/index.ts
业务逻辑 + 本地SQLite数据库（/workspace/server/local.db）
```

**旧架构淘汰原因**：
1. Pinggy免费隧道60分钟强制断开，导致频繁1016错误
2. 隧道带宽限制导致网站响应慢（点击后3-5秒才有反应）
3. 需要维护多个守护脚本（tunnel-keeper、never-die-guard、keep-alive）
4. 本地服务器环境不稳定（node_modules被定时清空）

### 3.2 文件上传链路（和普通请求不一样！）

```
用户浏览器 → 直接 PUT 到 Supabase Storage 签名URL（不经过上面整条链路）
                  ↑
                  |
       前端先调 /api/upload/presign → 后端生成签名URL返回给前端
```

**原因**：Pinggy免费隧道对>100KB文件返回520错误，文件直传完全绕过隧道，最稳。

### 3.3 【历史架构】进程守护架构（已废弃）

> 以下为旧架构的进程守护方案，Cloudflare 无服务器架构下不再需要这些脚本。脚本代码保留在仓库中供参考。

```
start-all.sh（/workspace/start-all.sh）
├─ 守护进程①：Node.js 后端  tsx server/index.ts
│     日志：/tmp/zhuiguang-app.log
│     崩溃后：3秒后自动重启
├─ 守护进程②：隧道守护  tunnel-keeper.sh v4
│     日志：/tmp/tunnel-keeper.log
│     崩溃后：3秒后自动重启
│     功能：每55分钟主动重连pinggy隧道 + 写GitHub tunnel-url.txt
└─ 守护进程③：保活脚本  keep-alive.sh
      日志：/tmp/keep-alive.log
      功能：每5分钟 curl 一次本地和隧道，防止空闲断连
```

### 3.4 当前架构文件部署对应关系

| 部署目标 | 文件 | 部署方式 |
|---|---|---|
| Cloudflare Workers（后端API） | `worker-api.ts` | `npx wrangler deploy` |
| Cloudflare Pages（前端） | `dist/` | Git连接自动构建或 `npx wrangler pages deploy dist` |
| Cloudflare D1（数据库） | `schema.sql` | `npx wrangler d1 execute zhuiguang-db --remote --file=schema.sql` |
| Workers配置 | `wrangler.toml` | 部署时自动读取 |
| 前端API地址配置 | `src/api/http.ts` | 构建时通过 `VITE_API_BASE_URL` 环境变量注入 |

---

## 4. 技术栈 & 依赖版本（禁止随意更换）

### 4.0 当前生产技术栈（v2.0 Cloudflare架构）

| 层 | 技术 | 版本 | 说明 |
|---|---|---|---|
| 后端框架 | Hono | 4.x | 轻量级 Web 框架，原生支持 Cloudflare Workers |
| 运行时 | Cloudflare Workers | - | 无服务器边缘计算，10ms CPU/请求（免费套餐） |
| 数据库 | Cloudflare D1 | - | SQLite 兼容，500MB 上限（免费套餐），500万行读/天 |
| 前端托管 | Cloudflare Pages | - | 全球 CDN，无限请求，自动 HTTPS |
| 文件存储 | Supabase Storage | - | 前端直传 presign URL |
| 部署工具 | Wrangler CLI | 3.x | `npx wrangler deploy` 部署 Workers |

### 4.1 【历史架构】本地技术栈（仍保留在仓库中）

| 层 | 技术 | 版本 | 禁止更换理由 |
|---|---|---|---|
| 语言 | TypeScript | 5.6.3 | 统一类型系统 |
| 前端框架 | Vue 3 | 3.5.12 | 全部组件已开发 |
| UI组件库 | Element Plus | 2.8.4 | 所有页面用它 |
| 状态管理 | Pinia | 2.2.4 | 用户/学科/班级/配置缓存 |
| 路由 | Vue Router | 4.4.5 | |
| 构建工具 | Vite | 5.4.10 | 单页应用构建 |
| Excel处理 | xlsx (SheetJS) | 0.18.5 | 数据查询导入+用户批量导入 |
| 图表 | echarts | 5.5.1 | 后台看板 |
| Markdown渲染 | marked | 18.0.7 | 美文/公告内容渲染 |
| HTTP请求 | axios | 1.19.0 | 前端统一请求封装 |
| 后端框架 | Express | 5.2.1 | 全部API路由 |
| 后端运行器 | tsx | 4.23.5 | 直接跑TS，不编译 |
| 密码哈希 | bcryptjs | 3.0.3 | 用户密码 |
| JWT | jsonwebtoken | 9.0.3 | 登录鉴权HS256 |
| 数据库驱动 | @libsql/client | 0.14.0 | Turso+本地SQLite通用驱动 |
| Supabase SDK | @supabase/supabase-js | 2.45.0 | 后端生成签名用 |
| 文件上传接收 | multer | 2.2.0 | 仅头像上传（其他直传Supabase） |
| CORS | cors | 2.8.6 | |
| 进程并发启动 | concurrently | 10.0.4 | npm run dev 同时起前后端 |

### 4.1 Node版本要求

- **要求：Node >= 20.0.0**
- 当前服务器：`v24.15.0` 安装在 `/root/.nvm/versions/node/v24.15.0/`
- tsx 必须通过 `npx tsx` 调用，直接全局tsx可能找不到正确node

---

## 5. 数据库结构（禁止随意ALTER表）

当前生产数据库：Cloudflare D1（`zhuiguang-db`，ID: `996ab327-1a44-47fb-ac1d-5ab963dd04a5`）
【历史】本地SQLite文件路径：`/workspace/server/local.db`（已迁移至D1，保留作本地开发用）
D1建表脚本：`/workspace/schema.sql`（23张表 + 索引，与本地SQLite schema完全一致）
数据迁移脚本：`/workspace/scripts/migrate-to-d1.sh`（SQLite → D1）

> D1 与 SQLite 语法兼容，但以下差异需注意：
> - D1 不支持 `PRAGMA journal_mode` 等本地PRAGMA（仅支持 page_count/page_size）
> - D1 的 `lastInsertRowid` 通过 `r.meta.last_row_id` 获取（不是 `r.lastInsertRowid`）
> - D1 查询结果在 `r.results` 数组中（不是直接返回数组）
> - D1 的 `.first()` 返回单行或null，`.all()` 返回 `{results: [...]}`

### 5.1 核心表（23张表，含 article_comments 和 feature_flags）

#### 1. users 用户表

| 字段 | 类型 | 说明 | 约束 |
|---|---|---|---|
| id | INTEGER PK | 用户ID | 自增 |
| username | TEXT UNIQUE | 登录用户名 | 不可空，唯一 |
| password_hash | TEXT | bcrypt哈希后的密码 | 不可空 |
| real_name | TEXT | 用户真实姓名 | 不可空 |
| role | TEXT | 角色 | `STUDENT`/`TEACHER`/`SUPER_ADMIN`，默认STUDENT |
| email | TEXT | 邮箱 | |
| phone | TEXT | 手机 | |
| avatar | TEXT | 头像URL | 空串默认 |
| exp | INTEGER | 经验值 | 默认0 |
| level | INTEGER | 等级 | 默认1 |
| status | TEXT | 状态 | `active`/`disabled`，默认active；disabled后登录接口直接拒绝 |
| subject_id | INTEGER | 教师绑定学科ID | 可空，仅教师有值 |
| **last_active** | TEXT | 最后活跃时间 | **2026-08-04新增**，用于在线人数统计；每次API请求异步UPDATE |
| created_at | TEXT | 创建时间 | 本地时间 |

#### 2. classes 班级表

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | 自增 |
| name | TEXT | 班级名称（如"高二（1）班"） |
| grade | TEXT | 年级（如"高二"） |
| description | TEXT | 描述 |
| created_at | TEXT | |

#### 3. class_members 班级-用户关联

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | |
| class_id | INTEGER FK→classes | 班级ID |
| user_id | INTEGER FK→users | 用户ID |
| role_in_class | TEXT | `STUDENT`/`TEACHER` |
| subject_id | INTEGER | （可选）教师在该班级任教学科 |
| joined_at | TEXT | |

#### 4. subjects 学科表

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | |
| name | TEXT | 学科名（如"语文"） |
| slug | TEXT UNIQUE | 英文标识（如"chinese"） |
| icon | TEXT | emoji图标（如"📖"） |
| color | TEXT | 主题色hex（如"#ef5350"） |
| description | TEXT | 学科描述 |
| display_order | INTEGER | 排序 |
| modules | TEXT(JSON) | 该学科开启的模块开关 |
| announcement | TEXT | 学科页顶部公告 |

#### 5. articles 美文/资料文章表

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | |
| title | TEXT | 标题 |
| content | TEXT | Markdown正文 |
| author / source / recommendation | TEXT | 展示字段 |
| subject_id / user_id / class_id | INTEGER | 归属；user_id=**在前端显示的操作人**（审核通过不改它！） |
| **actual_user_id** | INTEGER | **2026-08-04新增**：实际作者学生ID，教师/超管代发模式时填；pending_student状态时由这个学生确认 |
| cover | TEXT | 封面图URL |
| images | TEXT(JSON数组) | 正文插图 |
| tags | TEXT(JSON数组) | |
| category | TEXT | 分类 |
| status | TEXT | `pending`/`approved`/`rejected`/**`pending_student`**（2026-08-04新增，待学生确认） |
| likes / views | INTEGER | 计数 |
| created_at | TEXT | |

**业务规则（2026-08-04强化版）**：
- SUPER_ADMIN 自己发：直接 approved；**代发（给学生）+actualUserId时走TEACHER代发逻辑**
- TEACHER 自己发：直接 approved
- TEACHER 代发（传了actualUserId且不是自己）：`status='pending_student'`，发站内信+消息给actualUserId学生，由学生点确认后变为pending再等超管审核
- STUDENT 自己发：`status='pending'`，等超管/教师审核
- **审核绝不改user_id**：审核只是改status字段，作者永远是原始上传者（Bug1修复点）
- 可见性：非approved状态的文章只有：本人user_id、actual_user_id、对应学科教师、超管能看到；其他人列表中不出现、详情404（Bug7修复点）

#### 6. resources 资源/资料文件表

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | |
| subject_id | INTEGER | 学科 |
| title | TEXT | 标题 |
| description | TEXT | 描述 |
| file_name | TEXT | 原始文件名 |
| file_type | TEXT | `pdf`/`ppt`/`word`/`excel`/`zip`/`video`/`image`/`file` |
| file_size | INTEGER | 字节 |
| file_path | TEXT | Supabase public URL 或 storage key |
| category / tags(JSON) | TEXT | |
| user_id / class_id | INTEGER | |
| status | TEXT | `pending`/`approved`/`rejected`（审核规则同articles） |
| downloads / likes / collects | INTEGER | 计数 |
| version | INTEGER | 默认1 |
| created_at | TEXT | |

#### 7. exp_logs 经验值流水表

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | |
| user_id | INTEGER FK | |
| action_type | TEXT | 行为类型 |
| exp_change | INTEGER | 增减（负数扣） |
| description | TEXT | 说明 |
| created_at | TEXT | |

**所有合法action_type（在 settings.exp_rules 配分值）**：
- `login` 每日首次登录
- `register` 注册
- `article` 发布美文
- `resource` 上传资料
- `quiz_pass` 通过测验
- `article_like` 被点赞
- `comment` 评论
- `query_view` 查看成绩查询
- `practice_correct` 单题训练答对
- `admin_adjust` 管理员手动调整

**登录积分每日去重逻辑已修复**：同一天重复登录只发一次。判断：查当天 substr(created_at,1,10)=YYYY-MM-DD AND action_type='login' 是否已有记录。

#### 8. notices 站内信/通知

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | |
| user_id | INTEGER FK | 接收人 |
| title / content | TEXT | 标题和正文 |
| type | TEXT | `message`/`system`/`approval` 等 |
| read | INTEGER | 0未读/1已读 |
| created_at | TEXT | |

**站内信会话已修复**：/api/messages/sessions 接口对所有用户返回 `{sessions, allUsers}` 两部分，allUsers是全部活跃用户列表（除自己），前端取它做联系人选择，避免普通用户看不到别人发不了消息。

#### 9. themes 主题表
#### 10. likes_map 点赞/收藏统一表（唯一键 user_id+target_type+target_id）
#### 11. quizzes / quiz_questions / quiz_submissions 题库自测
#### 12. subject_questions / practice_submissions 单题训练
**practice_submissions 字段说明**：`id, question_id, user_id, answer, score, max_score, status('pending'|'graded'), correct(0|1), comment, submitted_at, graded_at`。其中 `status='graded'` 表示教师已批改主观题；选择题/填空题学生提交后立即自动判分，status='graded'，correct=1/0。

**经验值 action_type**：单题训练批改完成用 `practice_pass`（+5），题库自测用 `quiz_pass`（+10），两者不同。
#### 13. query_tasks / query_rows 教师批量导入Excel成绩查询
#### 14. pages / page_comments 通用页面 / 博客 / 公告 / 网站说明
#### 15. settings 全局设置表（单行配置，含 exp_rules 经验规则、feature_flags 功能开关 JSON 大字段）
#### 16. messages 私信表（from_id, to_id, content, attachments JSON, is_read）
#### 17. **article_comments 美文评论表**（2026-08-04 Bug2新增）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | 自增（**JSON返回时必须Number()转，避免BigInt序列化错误**） |
| article_id | INTEGER FK → articles | 评论所属美文 |
| user_id | INTEGER FK → users | 评论人 |
| user_name | TEXT | 冗余的评论人显示名 |
| avatar | TEXT | 冗余的评论人头像URL |
| content | TEXT | 评论文本 |
| created_at | TEXT | CURRENT_TIMESTAMP |

**修复Bug2注意**：libsql返回的 `lastInsertRowid` 是 `bigint` 类型，`res.json()` 会抛 `BigInt serialize error`。**评论接口必须** `Number(r.lastInsertRowid)` 显式转number再返回。

#### 18. **feature_flags 功能开关KV表**（2026-08-04 Bug5新增，独立于settings的feature_flags JSON字段，便于按key查询和前端public接口读）

| 字段 | 类型 | 说明 |
|---|---|---|
| key | TEXT PK | 开关唯一key，如 `registration_enabled` |
| value | TEXT | '0'=关闭，'1'=开启（用字符串存避免SQLite布尔兼容问题） |

**当前已使用的key**：
- `registration_enabled`：自助注册开关（默认'1'开启）。**后端helpers.ts getFeatureFlags()会把这个KV表的值合并进settings.feature_flags，KV是权威源**；前端登录页通过 `GET /api/feature-flags/public` 读，不需要鉴权。

**为什么要分两个地方？**
- settings.feature_flags JSON：存quiz/blog/guide/leaderboard等旧开关，整体PUT整体更新，admin专用
- feature_flags KV表：专门存注册开关registration_enabled这种public需要读的；`GET /api/feature-flags/public` 不需要登录，登录页就能调用
- PUT /api/settings/feature_flags 时会把registration_enabled同步写入KV表（server/index.ts L1112附近）

---

## 6. 核心文件索引（改哪件事看哪份文件）

### 6.1 后端

| 任务 | 文件 | 关键行/函数 |
|---|---|---|
| **【当前生产】Cloudflare Workers API** | **worker-api.ts** | **Hono框架，115+路由，D1数据库操作，从server/index.ts转换** |
| **【当前生产】Workers部署配置** | **wrangler.toml** | **D1 binding、环境变量、自定义域名** |
| **【当前生产】D1建表脚本** | **schema.sql** | **23张表 + 索引** |
| **【当前生产】数据迁移脚本** | **scripts/migrate-to-d1.sh** | **SQLite → D1 数据迁移** |
| 【历史】所有API路由总入口 | server/index.ts | 全文件，按功能分组（已转换为worker-api.ts） |
| 数据库连接+建表+初始化种子 | server/db.ts | initDB()（本地开发用） |
| 鉴权中间件+角色装饰器 | server/auth.ts | auth(), requireStaff(), requireRole(), canManageSubject() |
| 业务辅助函数（addExp、等级计算） | server/helpers.ts | addExp(), computeLevel() |
| 文件存储封装（Supabase + 本地上传双模式） | server/storage.ts | createPresignedUploadUrl(), uploadFile() |
| JWT签发+用户公开字段过滤 | server/auth.ts → signJwt(), pub() | |

### 6.2 前端

| 任务 | 文件 | 关键 |
|---|---|---|
| 前端总路由配置 | src/router/index.ts | 所有路由、权限守卫（超管/admin/role.requiresStaff） |
| 统一请求封装 + 所有API方法 | src/api/index.ts | api.xxx() 所有方法 |
| 用户/学科/班级全局store | src/store/data.ts | fetchUsers/fetchClasses/fetchSubjects |
| 用户管理界面（含批量导入） | src/views/admin/UsersView.vue | downloadTemplate / handleImportUpload / confirmImport |
| 数据查询（Excel导入成绩） | src/views/admin/QueryCreateView.vue | 前端xlsx解析参考 |
| 美文编辑（封面+插图上传） | src/views/ArticleEditView.vue | 直传模式 |
| 站内信（联系人列表修复） | src/views/MessagesView.vue | 用 r.allUsers 作为联系人 |
| 资料共享（上传弹窗） | src/views/SubjectView.vue | 资源Tab |
| 题库编辑（附件上传） | src/views/quiz/QuizEditView.vue | |
| 单题作答页 | src/views/quiz/PracticeTakeView.vue | 学生作答选择题/填空题（即时判分）+ 主观题（提交待批） |
| 我的训练记录 | src/views/quiz/PracticeRecordsView.vue | 学生分页查看自己的训练提交（含得分/评语/时间），支持删除 |
| 单题统计页 | src/views/quiz/PracticeStatsView.vue | 教师/超管按题目查看全班统计（正确率/待批/得分分布），支持删除记录 |

### 6.3 运维/部署

| 任务 | 文件 |
|---|---|
| **【当前生产】Cloudflare Workers部署** | **wrangler.toml + worker-api.ts** |
| **【当前生产】Cloudflare Pages前端** | **dist/（通过Git自动构建或wrangler pages deploy）** |
| **【当前生产】D1数据库管理** | **schema.sql + scripts/migrate-to-d1.sh** |
| 【历史】全量启动（后端+隧道+保活） | start-all.sh（已废弃，不再用于生产） |
| 【历史】一键更新（拉GitHub+构建+重启+验证） | update.sh（已废弃，Cloudflare自动部署） |
| 【历史】隧道守护（pinggy重连+GitHub更新） | tunnel-keeper.sh（已废弃） |
| 【历史】Cloudflare反向代理代码 | worker.js（已废弃，被worker-api.ts取代） |
| 【历史】自托管保活 | keep-alive.sh（已废弃） |
| 【历史】不死守护进程 | never-die-guard.sh（已废弃） |
| 【历史】Docker部署 | Dockerfile + .dockerignore（备用） |
| Vite构建配置 | vite.config.ts |
| 环境变量（敏感） | .env（不要提交git）/ wrangler.toml [vars] |

#### ⭐ 代码发布流程 v2.0（Cloudflare架构，新AI必读！）

> **新AI改完代码后，按以下流程发布，用户才能在网站上看到变化。**

```
新AI改代码（在本地/IDE）
  ↓
新AI执行 git push 到 GitHub 仓库 wrm120318/zhuiguang 的 main 分支
  ↓
分两种情况：
  ├─ 改了前端代码（src/*.vue, src/*.ts）
  │    ↓ Cloudflare Pages 自动检测Git push，自动构建部署
  │    ↓ 用户刷新 https://xkzg.dpdns.org → 看到新前端
  │
  └─ 改了后端代码（worker-api.ts）
       ↓ 需要手动部署到Cloudflare Workers
       ↓ cd /workspace && npx wrangler deploy
       ↓ 用户刷新 → API生效
```

**前端发布（自动）**：
- Cloudflare Pages 已连接 GitHub 仓库，push 到 main 分支自动触发构建
- 构建命令：`npm install && npm run build`
- 输出目录：`dist`
- 环境变量 `VITE_API_BASE_URL` 需在 Pages 项目设置中配置

**后端发布（手动）**：
```bash
source /workspace/.env
cd /workspace
CLOUDFLARE_API_TOKEN="$CLOUDFLARE_API_TOKEN" \
CLOUDFLARE_ACCOUNT_ID="$CLOUDFLARE_ACCOUNT_ID" \
npx wrangler deploy
```
- Wrangler 会读取 wrangler.toml 配置，部署 worker-api.ts 到 Cloudflare Workers
- 凭证值在 /workspace/.env 中（不入库），先 `source .env` 加载

**数据库变更（手动）**：
```bash
# 执行SQL
npx wrangler d1 execute zhuiguang-db --remote --command="YOUR SQL HERE"
# 或执行SQL文件
npx wrangler d1 execute zhuiguang-db --remote --file=your_migration.sql
```

---

## 7. 文件上传架构（最重要的设计，90%的上传Bug都和它有关）

### 7.1 为什么不用传统multipart上传？

【历史原因】Pinggy免费隧道对**超过100KB**的请求会返回 `520 Origin Error`，直接废掉。

【当前状态】迁移到Cloudflare Workers后，理论上不再有100KB限制（Workers单请求 body 上限100MB）。但 presign 直传架构仍然保留，原因：
1. 前端直传Supabase不消耗Workers CPU时间（免费套餐10ms/请求）
2. 大文件不经过Workers转发，减少延迟
3. 架构已经稳定运行，无必要改回multipart

### 7.2 当前正确模式：前端直传 Supabase + 后端只存元数据

```
步骤1（后端生成签名）：
  前端 → POST /api/upload/presign {fileName, contentType, folder?}
         → 后端 server/index.ts L202-L231
         → server/storage.ts createPresignedUploadUrl()
         → Supabase生成一个60秒内有效的 signed PUT URL
         → 返回 { signedUrl, key, publicUrl, type, fallback:false }

步骤2（前端直接PUT，绕过隧道）：
  前端 → 直接 PUT 文件二进制到 signedUrl
         → 不走本项目后端，更不走pinggy隧道
         → 上传成功拿到 Supabase 返回 200 OK

步骤3（写数据库记录）：
  前端 → POST /api/resources / POST /api/articles 等
         → 把 publicUrl、key、文件大小、类型等发给后端存数据库
```

**代码位置**：前端直传封装在 src/api/index.ts → `directUpload(file, opts)`

### 7.3 el-upload 组件的绝对禁忌

**所有页面的 el-upload 必须用 `:http-request` 自定义上传，绝对不要用 `:before-upload` 钩子！**

原因：before-upload 里面执行异步操作（等presign、等直传），返回 Promise 时 Element Plus 会静默吞掉失败，用户"选完文件没反应，控制台没报错"，是最恶心的bug。

正确写法（各页面一致）：
```vue
<el-upload
  :http-request="customRequest"
  :show-file-list="false"
  accept="xxx">
  <el-button>上传文件</el-button>
</el-upload>

<script setup>
async function customRequest(option: any) {
  try {
    ElMessage.loading('上传中...')
    const { url } = await api.uploadFile(option.file)
    // 写数据库或更新表单值
    ElMessage.success('上传成功')
  } catch (e: any) {
    ElMessage.error(e?.message || '上传失败')
  } finally {
    ElMessage.closeAll()
  }
}
</script>
```

### 7.4 需要改上传逻辑时的文件清单（漏一个就有bug）

- 资料共享上传：src/views/SubjectView.vue（资源Tab上传弹窗）
- 美文封面图+插图：src/views/ArticleEditView.vue, BlogEditView.vue, AnnouncementEditView.vue
- 题库附件：src/views/quiz/QuizEditView.vue（题目attachments）
- 数据查询Excel上传+导入：src/views/admin/QueryCreateView.vue（前端本地解析，不上传服务器）
- 用户批量导入：src/views/admin/UsersView.vue（前端本地解析xlsx，调后端importUsers）
- 头像上传：src/api/index.ts uploadAvatar()（用multer走后端，因为小文件，走presign直传也可以）

### 7.5 浏览器缓存控制（已修复，不要删）

每次 `npm run build` 后用户浏览器会缓存旧的 `index-XXXX.js`，改完代码不生效。

**已修复方案**：server/index.ts L30-L46，后端第一次请求 dist/index.html 时给所有 `<script src>` 和 `<link href>` 追加 `?v=时间戳`。这段代码绝对不能删！已经碰到过无数次"我改了为什么用户那边还是老样子"。

---

## 8. 角色与权限体系

### 8.1 三个角色（不要加新角色，除非业务强制）

| 角色 | role字段值 | 权限 |
|---|---|---|
| 超级管理员 | SUPER_ADMIN | 全站所有操作，包括用户管理、经验调整、班级/学科增删改 |
| 教师 | TEACHER | 管理任教学科的美文/资料/题库/成绩查询、审核学生上传、发布公告 |
| 学生 | STUDENT | 浏览、上传资料/美文（待审核）、做题、查成绩、站内信 |

### 8.2 权限中间件（server/auth.ts）

```ts
auth                    // 只要登录（有JWT）
requireStaff            // TEACHER 或 SUPER_ADMIN 通过
requireRole('XXX')      // 精确匹配角色
canManageSubject(user, subjectId)  // 超管通过；教师仅自己任教学科通过
```

---

## 9. 核心业务规则（业务逻辑红线，不能改）

1. **经验值等级系统**：行为→加分→触发升级，分值在 settings 表 exp_rules JSON 字段里配置。调整请直接改库或用后台"经验调整"功能，不要硬写死在代码里。
2. **美文/资料审核流**：超管/教师 → 自动过审（approved）；学生 → 待审核（pending）；前端列表加载时叠加自己pending态的条目，"自己能看到自己发的待审核"。
3. **站内信双向可见**：普通学生也能看到全部活跃用户并主动发起消息，不是只能等超管找你。
4. **登录积分每日一次**：exp_logs 同日期同用户同`login`只存一条，用户多次登录不重复发分（已修复，之前是登一次给一次）。
5. **成绩查询行级过滤**：学生查 query/:id 只能看自己匹配的那行数据（按 match_field 匹配姓名/学号），教师看全部。
6. **文件前端直传**：如第7章，除非新上传场景是小图标（<100KB），否则一律presign直传+后端存记录，不要加 POST /api/upload/file。

---

## 10. 所有已修复Bug清单（新AI不得改回去）

> ⚠️ **每个新维护者最容易犯的错："这里为什么这么奇怪？我重构一下" → 把修复好的Bug又改回去了！** 下面每一条都是曾真实踩过的坑，不要动它们的代码形态。

| ID | Bug现象 | 根因 | 修复方法 | 代码位置 |
|---|---|---|---|---|
| BUG-01 | 用户选完上传文件后"没反应，控制台没报错" | el-upload用了before-upload，异步失败被Element吞 | 全部改为:http-request自定义上传，try/catch + ElMessage.error明确提示 | 所有含上传的.vue文件 |
| BUG-02 | 文件>100KB上传失败520 | pinggy免费隧道限制 | 后端加presign接口，前端直传Supabase，完全不走隧道 | server/index.ts L202-L231 + storage.ts + src/api/index.ts directUpload() |
| BUG-03 | 改完代码用户浏览器还是老界面白屏或报错 | 浏览器缓存旧index-XXXX.js | 后端启动时读dist/index.html给所有静态资源加?v=时间戳参数 | server/index.ts L30-L46（不要删！） |
| BUG-04 | 资料/美文上传后在列表里看不到 | 学生上传是pending态，列表SQL只WHERE status='approved' | 1. 教师/超管上传自动设为approved；2. 前端列表叠加当前用户自己pending的条目 | server/index.ts 资源/文章插入逻辑 + 各View加载函数 |
| BUG-05 | Error 1016 Origin DNS error 每小时出现一次 | pinggy60分钟强制断连，重连后域名变，Worker读缓存旧域名 | ①tunnel-keeper 55分钟主动重连+立即写GitHub；②Worker v4 5秒TTL+1016时立即强制刷新URL并自动重试一次+缓存降级30分钟+空串友好页；③隧道URL校验通过才写GitHub（失败3次重试，不行写空串兜底） | tunnel-keeper.sh v4 + worker.js v4 |
| BUG-06 | 站内信学生看不到联系人，不能给别人发消息 | /api/messages/sessions 原本只给超管返回allUsers，普通用户返回空 | 后端对所有用户都返回 allUsers=全部活跃用户（除自己），前端contacts=r.allUsers | server/index.ts 站内信会话接口 + MessagesView.vue L30 |
| BUG-07 | 每登录一次就加一次积分，一天登5次就加5次 | 登录接口每次都调addExp('login') | 先查exp_logs当天是否已login记录，有则跳过addExp | server/index.ts 登录接口addExp之前的判断 |
| BUG-08 | tunnel-keeper检测到本地服务偶尔一次超时就杀隧道重建（更频繁的1016） | 后端启动/GC/高并发，单请求超时正常 | 本地服务连续失败3次才算真挂，才触发隧道重启 | tunnel-keeper.sh v4 LOCAL_FAIL_LIMIT=3 |
| BUG-09 | 环境每次重置后node_modules被清空，后端报dotenv找不到崩溃 | node_modules不是持久化 | start-all.sh每次启动前检查dotenv/express是否存在，缺失自动npm install --production | start-all.sh 开头自动检查逻辑 |
| BUG-10 | Worker遇到旧失效域名时直接把Cloudflare 1016裸页甩给用户 | 没做内容检测就透传 | Worker v4 检测body是否含Error 1016/Origin DNS error/520-530 → 立即强制从GitHub拉新URL重试一次；还是失败就返回友好"重连中"页面10秒自动刷新 | worker.js v4 中段 looksLike1016 判断 + friendlyError_HTML |
| BUG-11 | 学生发美文→超管审核→作者被篡改成超管本人，经验错加到超管头上 | PATCH status时顺手UPDATE了user_id / addExp给了审核人 | 审核接口只UPDATE status不改user_id；发文经验绑定实际作者；审核经验给review动作 | server/index.ts PATCH articles/:id/status 只改status |
| BUG-12 | 美文共赏评论功能完全失效（接口500） | libsql返回lastInsertRowid是bigint，res.json()抛"Do not know how to serialize a BigInt" | 评论创建后 `Number(r.lastInsertRowid)` 显式转JavaScript number；统一article_comments表 | server/index.ts 评论POST接口 + db.ts建article_comments表 |
| BUG-13 | 美文审核形同虚设（点了状态不变/列表还是看到全部） | ①路由顺序问题：pending-student被/:id当参数吃掉404；②可见性没做行级过滤 | ①pending-student/student-approve/student-reject前移到/:id前；②列表/详情WHERE叠加可见性（自己/学科教师/超管） | server/index.ts L522-L560 路由顺序 + articles查询可见性判断 |
| BUG-14 | 账号禁用应该：正在使用立即弹窗踢下线+此后无法登录 | 登录接口只校验了bcrypt没判断users.status==='disabled' | POST /api/auth/login在密码通过后额外判断 `if (u.status === 'disabled') return 400 '账号已被禁用'`；前端axios拦截401/403消息里带"禁用"字样→ElMessageBox踢人清token | server/index.ts 登录接口status校验 + src/api/index.ts响应拦截器 |
| BUG-15 | 管理员应该可以在管理面板开关注册功能 | ①前端FeatureFlagsView.vue ALL_FLAGS漏了registration_enabled卡片；②后端没public接口供登录页读；③helpers.ts getFeatureFlags没合并KV表 | ①加🚪自助注册卡片+说明；②加GET /api/feature-flags/public（无鉴权）；③getFeatureFlags合并KV表的registration_enabled；④PUT settings/feature_flags时同步写回KV表 | FeatureFlagsView.vue ALL_FLAGS数组 + helpers.ts getFeatureFlags + server/index.ts public接口+同步写回 |
| BUG-16 | 学生确认美文发布功能完全不存在（应该是收到站内信富文本点确认→别人才能看） | 缺student-approve/student-reject接口；代发后没发站内信和富文本私信 | ①补两个POST接口（鉴权actual_user_id匹配+status==='pending_student'）；②代发后addNotice+INSERT messages富文本（带引导链接和附件action）；③前端在个人中心加"待我确认的美文"列表 | server/index.ts L534-L560 两个学生确认接口 + 代发后消息写入 |
| BUG-17 | 美文在未审核/未通过/未确认时没标注状态标签，且非相关人能看到（泄露） | ①articles查询没有统一JOIN出状态和实际作者名；②列表SQL没有可见性WHERE条件；③前端缺el-tag渲染 | ①统一SQL：JOIN users AS creator / actual_user拿显示名；②可见性OR链：(approved=所有人可见) OR (user_id=me OR actual_user_id=me OR (我是教师AND学科匹配) OR 我是超管)；③前端列表统一按status渲染颜色标签 | server/index.ts articles SELECT模板 + 各SubjectView/ArticleView el-tag color映射 |
| BUG-18 | 个人中心经验记录显示异常（白屏/undefined/接口404） | 前端调/exp-logs但后端路由是/exp/logs，字段名不一致 | 后端统一 `GET /api/exp/logs` 返回标准6字段数组；前端对齐字段名；查不到返回空数组不报错 | server/index.ts app.get('/api/exp/logs') + src/store或对应view |
| BUG-19 | 学科教师管理页看到了用户管理/功能开关/经验设置/删用户（只能看数据查询+本学科审核） | ①敏感接口用requireStaff放行教师了；②前端菜单/路由守卫没按v-if限制SUPER_ADMIN | ①/api/users全系列 /api/settings/* 写接口 /api/exp/rules写接口 → 统一requireRole('SUPER_ADMIN')；②前端admin下子路由meta.requiresSuperAdmin=true，路由守卫拦；③Sidebar教师角色下隐藏用户/设置菜单项 | server/index.ts敏感接口中间件 + src/router/index.ts路由守卫 + src/layouts/Sidebar.vue菜单v-if |
| BUG-20 | 用户访问https://xkzg.dpdns.org 几十分钟一直显示「服务正在重连中」=1016卡死（Worker v4永久返回友好页） | 两大根因叠加：①环境/容器定时清空node_modules → start-all依赖检测只查2个包→better-sqlite3等缺失→全进程崩溃→隧道URL没推GitHub；②Worker v4只走Raw CDN（raw.githubusercontent.com），而GitHub Raw CDN对文件有10-30分钟强缓存且不可手动清 | ①start-all.sh 依赖检测升级：16个关键包目录检测+缺就npm install+二次验证强制重装；②Worker v4→v5双路兜底：Raw CDN(快)+GitHub Contents API(实时绕过CDN)双路取URL，连续失败强制切API，1016时forceApiNext=true，再叠加lastGoodUrl30分钟降级缓存；③应急操作：换全新隧道URL触发v4的url≠cachedUrl清零failCount；④Secret Scanning防护：文档中Token永远脱敏写ghp_***不贴完整值 | start-all.sh L18-L52依赖加固 + worker.js v5 256行双路兜底 + 手动部署步骤见12.5节 |
| BUG-21 | 单题训练批改完成后经验值加了10而非5（与题库自测混淆） | server/index.ts 第1970行和 worker-api.ts 第2712行，单题训练批改完成时 addExp 的 action_type 写成了 'quiz_pass'（题库自测，+10），应使用 'practice_pass'（+5） | 将两处 addExp 的 action_type 从 'quiz_pass' 改为 'practice_pass'；后端 DEFAULT_EXP_RULES 已正确定义 practice_pass:5 / quiz_pass:10，无需改规则 | server/index.ts L1970 + worker-api.ts L2712 |
| BUG-22 | 单题训练教师批改功能完全失效 + 删除记录接口重复定义 | ①PracticeStatsView点"去批改"跳`/practice/:id?grade=subId`但PracticeTakeView完全没有批改UI（无打分表单、无学生作答展示）→ 教师批改主观题流程被打断；②server/index.ts和worker-api.ts中DELETE /api/practice/record/:id重复定义两次，Express/Hono用第一个（仅本人），教师删学生记录直接403；③批改时需要查看学生作答内容，但没有取单条提交详情的接口 | ①PracticeTakeView.vue新增isGrading模式(通过route.query.grade判断)，加载practiceSubmission(subId)获取学生作答+题目，渲染批改表单(打分+评语+删除)；②删除接口合并为统一版本(本人OR教师本学科OR超管)；③新增GET /api/practice/submission/:id接口（含权限校验） | PracticeTakeView.vue + worker-api.ts L2755 + server/index.ts L2012 |

---

## 11. 所有已完成功能清单

| 模块 | 功能 | 完成时间 |
|---|---|---|
| 账户 | 注册/登录/JWT鉴权/头像上传/密码重置 | 早期 |
| **账户（2026-08-04强化）** | **禁用=登录直接拒绝+前端弹踢下线提示（Bug14）+ 自助注册开关（Bug15）** | **2026-08-04** |
| 用户管理（超管） | 用户增删改查/批量导入Excel模板+上传/启禁用/重置密码/调整经验/等级 | 2026-08-04 |
| 班级/学科管理 | 班级学科CRUD/教师任教学科绑定/模块开关/学科公告 | 早期 |
| 美文共享 | 发布/编辑/审核/Markdown渲染/点赞/分类标签/我的/待审核我可见 | 早期 |
| **美文共享（2026-08-04大改）** | **评论功能（Bug12新建article_comments表）+ 审核不改作者（Bug11）+ 状态标签行级可见（Bug17）+ 教师代发→学生确认（Bug13+Bug16 pending_student流程+站内信富文本通知）+ 审核通过状态真正生效（Bug13）** | **2026-08-04** |
| 资料共享 | 上传/分类/下载计数/文件类型筛选/点赞/收藏/我上传的待审核可见 | 早期+修复 |
| 题库自测 | 题库/题目/多题型单选多选判断填空/附件/限时/得分/自动判分 | 早期 |
| 单题训练 | 学科题目池/逐题练习/对错反馈/老师评语/做题统计 | 早期 |
| **单题训练（2026-08-10强化）** | **教师批改模式：PracticeTakeView.vue支持?grade=subId批改主观题（打分/评语/查看作答/删除记录）；删除接口合并为统一权限(本人OR教师/超管)；新增GET /api/practice/submission/:id接口** | **2026-08-10** |
| 数据查询（成绩） | 教师前端本地解析Excel导入/按姓名学号匹配行/学生只能看自己的行 | 早期 |
| 站内信 | 会话列表/发送消息/联系人列表全部用户/未读提醒 | 早期+BUG修复 |
| **站内信（2026-08-04追加）** | **代发美文后自动发送富文本私信+附件action引导学生点确认（Bug16）** | **2026-08-04** |
| 全局设置 | 经验规则EXP_RULES配置/网站说明/全局公告/主题 | 早期 |
| **全局设置（2026-08-04追加）** | **功能开关新增registration_enabled自助注册卡片；feature_flags新增KV双写模式；经验记录接口标准化（Bug18）** | **2026-08-04** |
| 看板 | 用户/资料/成绩查询统计+ECharts图表 | 早期 |
| 文件上传架构 | 全量Supabase直传，绕过pinggy隧道100KB限制 | 中期重构 |
| 部署稳定性 | start-all守护/tunnel-keeper v4/Worker v4友好页/保活脚本 | 2026-08-04终版 |
| 经验值系统 | 行为加分/自动升级/每日登录去重发放 | 早期+BUG修复 |
| **经验值（2026-08-04强化）** | **审核通过他人美文经验给原作者不给审核管理员（Bug11附加）+ review加分项；last_active用户活跃追踪** | **2026-08-04** |
| **权限体系（2026-08-04大改）** | **学科教师仅看数据查询+本学科审核；用户管理/功能开关/经验规则/删用户→强制SUPER_ADMIN（Bug19）；前端菜单+路由守卫双重限制** | **2026-08-04** |
| **部署稳定性（2026-08-05加固BUG-20）** | **start-all依赖16个关键包缺失自动重装+二次验证强制兜底；Worker v4→v5双路取URL（Raw CDN+GitHub Contents API绕过CDN缓存）；连续失败自动降级30分钟lastGoodUrl兜底** | **2026-08-05** |
| **流程与合规（2026-08-05升级）** | **所有方案全免费不绑卡不试用；联网引用必须附真实URL；文档Token永久脱敏；工作日志+文档更新记录双轨制** | **2026-08-05** |

---

## 12. 运维与部署指南（服务器/Cloudflare）

### 12.1 启动/停止/重启整个服务

```bash
# 【启动】（推荐，崩溃会3秒自动重启）
cd /workspace
nohup bash start-all.sh > /tmp/zhuiguang-start.log 2>&1 &

# 【停止】
ps aux | grep -E "start-all|tsx server|tunnel-keeper|keep-alive" | grep -v grep | awk '{print $2}' | xargs kill

# 【重启】= 停止 + 启动
```

### 12.2 本地开发（改代码时用）

```bash
cd /workspace
npm run dev
# 会同时开：后端 tsx watch（3001端口） + Vite前端（5173端口）
# 浏览器开 http://localhost:5173 ，Vite自动把 /api 代理到3001
```

### 12.3 重新构建前端（改完.vue/.ts要发布给用户）

```bash
cd /workspace
npm run build
# 等10-20秒，完成后刷新网站就看到新代码了
# （server/index.ts里有自动加?v=时间戳，缓存不会卡）
```

### 12.4 查看日志排错

```bash
# 后端日志
tail -f /tmp/zhuiguang-app.log

# 隧道守护日志（看1016/重连情况）
tail -f /tmp/tunnel-keeper.log

# 保活脚本日志
tail -f /tmp/keep-alive.log

# 当前使用的隧道URL
cat /tmp/tunnel-url-current

# 当前运行进程数（应为7-10个：3个guard+后端+n个ssh隧道）
ps aux | grep -E "tsx|tunnel-keeper|keep-alive|ssh.*pinggy" | grep -v grep | wc -l
```

### 12.5 Cloudflare Worker 手动更新

> GitHub代码变了，但Cloudflare不会自动同步，要手动操作。

1. 登录 Cloudflare → 进入 xkzg.dpdns.org 域名 → Workers & Pages
2. 点 zhuiguang-proxy Worker → Edit code（编辑代码）
3. 把 `/workspace/worker.js` 全部内容复制粘贴进去（全选替换）
4. 点 Save and deploy（保存并部署）

### 12.6 Docker部署（备用）

适用于 Back4App/Zeabur 等支持Docker的平台：
- Dockerfile直接用仓库预构建的dist/，不在容器里build，避免512MB内存构建Vue OOM
- 容器默认PORT=3000（不要改成3001，平台会注入PORT环境变量）

---

## 13. 常用命令速查

| 目的 | 命令 |
|---|---|
| 本地API是否活着 | curl http://localhost:3001/api/pages/guide（看HTTP 200） |
| 当前隧道URL | cat /tmp/tunnel-url-current |
| 隧道直连是否通（带代理） | curl -x http://127.0.0.1:18080 $(cat /tmp/tunnel-url-current)/api/pages/guide |
| GitHub tunnel-url.txt现在是啥 | curl -x http://127.0.0.1:18080 https://raw.githubusercontent.com/wrm120318/zhuiguang/main/tunnel-url.txt |
| 重新建前端 | npm run build |
| 数据库备份 | cp /workspace/server/local.db /workspace/server/local.db.bak.$(date +%Y%m%d) |
| 数据库恢复 | 先停服务 → cp backup /workspace/server/local.db → 启动 |

---

## 14. 故障排查手册（遇到1016/白屏/上传失败怎么办）

### 故障 A：用户打开网站显示 Error 1016 Origin DNS error

> **严重等级：中**
> **用户感知：** 如果Worker v4在生效，用户看到的应该是友好"服务正在重连中10秒后自动刷新"，不是1016裸页。如果看到的是1016裸页 → Worker没部署到v4。

**排查步骤**：
1. 看隧道守护是否在跑：`ps aux | grep tunnel-keeper`（应该有）
2. 看当前隧道URL：`cat /tmp/tunnel-url-current`
3. 隧道直连测一下（带上代理）：`curl -x 127.0.0.1:18080 <URL>/api/pages/guide`
4. GitHub同步测一下：第12.6条命令
5. 如果步骤3-4URL一致且都HTTP200 → 是Cloudflare Worker缓存没刷新 → 等待10秒或手动访问GitHub raw触发刷新
6. 如果隧道URL变了，GitHub还没写 → 看隧道日志 `tail -f /tmp/tunnel-keeper.log`，看 GitHub更新是否失败（通常是GITHUB_TOKEN过期，去GitHub发新token写到.env并重载）

**应急处理**：`start-all.sh 重启` 整个流程。

### 故障 B：用户那边还是旧代码白屏（刚发版）

**原因**：极少数浏览器无视 ?v= 参数强缓存旧的 index-xxx.js
**处理**：
1. 让用户 Ctrl+Shift+R 强制刷新 / 手机浏览器清缓存
2. 清 Cloudflare 全站缓存：Cloudflare控制台 → Caching → Configuration → Purge Everything

### 故障 C：选完文件没反应 / 上传失败

**先看浏览器F12控制台Network标签**：
- presign接口（/api/upload/presign）是否正常？
- PUT到supabase的请求有没有CORS/403？
- 报错是什么？

常见根因：
1. SUPABASE_SERVICE_KEY过期/错了 → 检查.env并重启后端
2. Supabase Bucket的CORS没开 → 后台看Cross-Origin Resource Sharing配置，允许xkzg.dpdns.org
3. 有人用before-upload改回去了 → 改回:http-request

### 故障 D：后端服务反复崩溃 code 127/xxx

看日志 `tail -f /tmp/zhuiguang-app.log`
- Cannot find module 'dotenv'/express → node_modules没了，start-all.sh会自动装；没装手动`npm install`
- **经验值action_type不能混用**：`practice_pass`(+5)是单题训练，`quiz_pass`(+10)是题库自测，改了会破坏已有经验值记录
- JWT_SECRET undefined → .env没加载，检查start-all.sh的export是否执行
- 端口被占用：`lsof -i :3001` kill掉旧进程再重启

---

## 15. 给新维护者的严格约束（必须遵守）

> 下面的每一条都是用真实Bug和服务器小时摸出来的经验教训。**如果违反，大概率会让项目出问题，或者把已经修好的Bug再改回去。**

### 15.1 绝对禁止事项（红线）

1. **禁止改文件上传架构**：不要把presign直传改回后端multipart，除非你换到了一个不限制100KB的隧道或服务器。
2. **禁止改 el-upload 模式**：不要把 :http-request 改回 :before-upload。
3. **禁止删 server/index.ts L30-L46 的缓存版本号注入**。
4. **禁止修改数据库字段/删表**：SQLite/Turso几乎不支持ALTER COLUMN，除非你知道自己在做什么并全量备份。想加字段加新表可以，但改现有字段/表名=灾难。
5. **禁止修改角色名称**：SUPER_ADMIN/TEACHER/STUDENT 三个字符串贯穿整个项目几百处判断，改了全崩。
6. **禁止提交 .env 到 git**：现在 .gitignore 应该已经排除了，确认。
7. **禁止把 JWT_SECRET 改掉**：一改所有用户全部掉线，必须重新登录。
8. **禁止在没有 .env 备份的情况下调整环境变量**。
9. **禁止随意换部署隧道/平台**：
   - cloudflared → 本服务器代理下超时
   - Render → 强制绑卡（风控）
   - Koyeb → 转型AI，不再做普通Web免费容器
   - Back4App → 连不上GitHub授权（之前尝试失败）
   - Glitch → 已关停新项目
   - Ngrok → 24小时重置域名，更差
   真想换先做一周稳定性测试再切，不要"我觉得应该行"直接线上切。
10. **禁止改 pinggy 55分钟主动重连为 60+ 分钟**：pinggy免费版60分钟强制断开，留5分钟缓冲是安全底线。

### 15.2 开发必须遵守的规则

1. 做改动之前，先通读本文档的【所有已修复Bug清单】，看看你要改的点是不是之前修过Bug，不要改回去。
2. 改动前备份 server/local.db：`cp server/local.db /tmp/local.db.bak`
3. 只做**最小化改动**，不要大范围重构。"顺手优化一下/改代码风格"都是引入Bug的原因。
4. 所有新增功能：改完先本地 `npm run dev` 测一遍，没问题 `npm run build` 再部署。
5. 涉及用户身份/权限/审核流程/积分的改动：必须用超管、教师、学生三个账号分别测一遍。
6. 涉及上传：必须用 >1MB 的PDF/图片/Excel都测一遍，不要只测10KB小文件。
7. 发版后至少观察5分钟：看网站能不能打开、登录后会不会白屏、各主要页面加载正常不报错。
8. 写代码时遇到"和现有实现风格不一样"的地方，**以现有实现为准**，不要引入新的依赖/新模式（比如想加Vue Composition宏换写法，想把axios换成fetch）。
9. 要新npm包时先搜 package.json dependencies，99%概率已经有了（做Excel看xlsx、做上传看multer、做加密看bcryptjs/jsonwebtoken）。

---

## 16. 项目发展时间线（构建到现在的完整历史）

| 时间 | 阶段 | 里程碑 / 事件 |
|---|---|---|
| 2026-07 下旬 | 立项 | 项目初始化：Vue3 + Express + Element Plus，单用户系统雏形，班级学科美文资料题库基础模块 |
| 2026-07 下旬 | 存储选型 | 接入 Turso libSQL 远程数据库 + Supabase Storage 文件存储，完成前后端基础架构 |
| 2026-07 下旬~8初 | 部署试错 | Render → 强制绑卡风控放弃；Koyeb → Mistral收购转型；Back4App → GitHub授权失败；各种部署平台碰壁 |
| 2026-08-01 | 部署落地方案确定 | 采用 Pinggy SSH免费隧道 + Cloudflare域名+Worker反向代理，本地Node跑 |
| 2026-08-01~03 | Bug大爆发 | 上传功能失效（100KB限制）、浏览器缓存旧代码、资料上传后不显示、登录积分重复发、站内信看不到联系人、频繁1016 DNS错误 |
| 2026-08-02 | 架构大重构：上传 | 实现 Supabase presign 前端直传架构，全量页面替换为 http-request 模式 |
| 2026-08-02 | 缓存问题修复 | server/index.ts 增加 dist/index.html 资源?v=时间戳注入 |
| 2026-08-03 | 隧道稳定性v1-v3 | tunnel-keeper 加入多服务器故障转移、指数退避、55分钟主动重连、GitHub URL更新 |
| 2026-08-03 | Worker v1-v3优化 | 缓存TTL15s→5s，失败模式2s，连续失败计数 |
| 2026-08-03 | 业务Bug修复 | 登录积分每日去重、站内信allUsers、资料/美文审核态我可见 |
| 2026-08-04 | 方案A终版落地 | tunnel-keeper v4（连续失败3次计数、软校验、GitHub3次重试+空串兜底）+ Worker v4（1016自动重试一次+友好故障页+降级缓存30分钟）+ start-all 自动补node_modules依赖 |
| 2026-08-04 | 新功能：批量导入用户 | 用户管理界面增加Excel模板下载+前端解析xlsx预览+后端批量入库（含用户名重复跳过+逐行错误提示） |
| 2026-08-04 上午 | 文档交接初版 | 撰写v1.0版完整交接文档，16大章节，准备移交下一任维护者 |
| **2026-08-04 下午** | **业务Bug9大修复 v1.1** | **用户一次性报告9条业务Bug（Bug11~Bug19），一次性全修复并10/10 API级回归验证通过；涉及：美文审核不改作者、评论BigInt、审核生效、账号禁用踢下线、注册开关、学生确认美文、状态标签+行级可见性、经验记录接口标准化、教师权限SUPER_ADMIN隔离** |
| **2026-08-04 下午** | **流程规范落地** | **新增《开发工作日志》文件；交接文档升级v1.1（补数据表/Bug清单/功能清单/时间线/文档更新记录）；确立「每次修复必须实机验证+写日志」流程** |
| **2026-08-05 凌晨** | **1016重连大修复 v1.2（BUG-20）** | **用户报告「几十分钟一直显示重连中」= 三层全崩溃+Raw CDN缓存滞留Worker v4死循环；修复：start-all依赖16包检测+Worker v4→v5双路兜底（Raw CDN+GitHub Contents API绕过CDN）+换全新隧道URL清零failCount；线上连续15轮HTTP200验证通过** |
| **2026-08-05 凌晨** | **流程升级（严格约束）** | **用户追加硬性要求：①所有解决方案必须全免费、不绑卡、不用付费试用；②联网获取信息必须引用真实URL（不瞎编）；工作日志/交接文档补合规审查条目；Secret Scanning拦截Token明文→永久脱敏规则** |
| **2026-08-05 全天** | **稳定性终极方案** | **实现 `/__zg_fix` 公开修复路由 + 登录页🔧按钮 + never-die-guard v3.0（15秒Python验真活URL）+ Worker v6.5.2（API边缘缓存+静态7天immutable）+ start-all依赖16包检测** |
| **2026-08-06~07** | **架构迁移决策** | **用户要求迁移到云端、全免费不绑卡。评估后选择 Cloudflare Workers + D1 + Pages 方案。创建 worker-api.ts（Hono框架，从Express转换115+路由）、schema.sql（D1建表）、wrangler.toml（配置）、migrate-to-d1.sh（数据迁移脚本）** |
| **2026-08-07** | **D1数据库创建+建表+数据迁移** | **创建D1数据库zhuiguang-db（ID: 996ab327-...），执行schema.sql建23张表，执行migrate-to-d1.sh将本地SQLite数据迁移到D1** |
| **2026-08-07** | **Workers部署+自定义域名** | **部署worker-api.ts到Cloudflare Workers（项目名zhuiguang-api），配置自定义域名 api.xkzg.dpdns.org** |
| **2026-08-07** | **Cloudflare Pages前端部署** | **前端dist/部署到Cloudflare Pages，配置自定义域名 xkzg.dpdns.org，设置VITE_API_BASE_URL环境变量指向Workers API** |
| **2026-08-07** | **监控仪表盘适配** | **重写MonitorView.vue适配Cloudflare无服务器环境：替换服务器CPU/内存指标为Cloudflare边缘节点信息、D1数据库统计、用户角色分布** |
| **2026-08-07** | **CORS跨域修复** | **自定义域名导致跨域问题：在worker-api.ts实现动态origin echo + credentials支持** |
| **2026-08-08** | **交接文档v2.0** | **整合Cloudflare Workers + D1迁移全部信息，创建GitHub全套配套文档（README/CONTRIBUTING/CHANGELOG/FAQ），更新AI启动提示词** |
| **2026-08-10** | **单题训练按题统计v2.1** | **修复单题训练经验值action_type混淆(BUG-21)；新增单题训练记录管理和按题统计功能：学生可查看全部训练记录（分页/删除），教师/超管可按单题查看全班统计（正确率/待批/得分分布/作答详情）** |
| **2026-08-10** | **单题训练批改模式v2.1.1** | **修复单题训练教师批改功能缺失(BUG-22)：PracticeTakeView.vue支持?grade=subId批改主观题；新增GET /api/practice/submission/:id接口；删除接口合并为统一权限版本** |
| **2026-08-10 下午** | **单题训练统计模块优化v2.1.2** | **移除统计接口无用scoreDist SQL查询和前端死代码(BUG-23)** |
| **2026-08-10 下午** | **单题训练通知补全v2.1.3** | **提交主观题同步发系统通知给教师/超管；批改主观题同步发系统通知给学生(BUG-25)** |
| **2026-08-10 下午** | **主观题批改correct字段修复v2.1.4** | **修复批改主观题后correct字段为null导致页面显示红色✗：在批改接口中补全correct字段计算写入（score>=max_score为1，否则0）** |
| **2026-08-10 下午** | **Admin经验设置默认值符号修复v2.1.5** | **修复ExpRulesView.vue中删除类行为默认值为正数(15/5/2/1)导致管理员保存设置后删除反加经验(BUG-27)：article_delete→-15,resource_delete→-15,blog_delete→-5,query_delete→-2,comment_delete→-1,like_cancel→-1** |

---

## 结尾：项目定位

追光不是互联网产品，不需要炫酷的技术栈，不需要微服务，不需要百万并发。它的核心使命是：

> **让一所中学的几十名师生，能稳定、免费、简单地共享学科资料和学习资源。**

能做到 24 小时里 23 小时 50 分钟能用，上传不失败，登录不发错积分，学生能看到老师发的资料，这就是成功。

永远不要为了"技术上更优雅"去动一个能跑的系统。"它能用"本身就是最高的技术标准。

祝交接顺利，下一任维护者好运。🍀

---

## 17. Cloudflare Workers + D1 迁移架构（v2.0 当前生产架构）

### 17.1 迁移背景

用户反馈旧架构（Pinggy SSH隧道）存在三大痛点：
1. 隧道60分钟强制断开，频繁出现 Error 1016
2. 网站响应慢（点击后3-5秒才有反应），用户体验差
3. 需要维护大量守护脚本（tunnel-keeper、never-die-guard、keep-alive、start-all），出问题需要人工干预

用户要求：全免费、不绑卡、不需要人工维护、网站能稳定运行。

### 17.2 新架构文件清单

| 文件 | 用途 | 关键内容 |
|---|---|---|
| `worker-api.ts` | Cloudflare Workers 后端 API | Hono 框架，115+ 路由，从 `server/index.ts` 完整转换，保留全部业务逻辑 |
| `wrangler.toml` | Workers 部署配置 | D1 binding、环境变量（JWT_SECRET、Supabase配置）、自定义域名 |
| `schema.sql` | D1 数据库建表脚本 | 19 张表 + 索引，与本地 SQLite schema 完全一致 |
| `scripts/migrate-to-d1.sh` | 数据迁移脚本 | 从 `server/local.db` 导出数据，通过 wrangler d1 导入到 D1 |
| `src/api/http.ts` | 前端动态 API 地址 | 通过 `VITE_API_BASE_URL` 环境变量注入 API 地址，支持本地开发和生产部署 |
| `src/views/admin/MonitorView.vue` | 监控仪表盘（重写） | 适配 Cloudflare 无服务器环境，显示 D1 数据库统计、边缘节点信息 |
| `dist/_redirects` | SPA 路由配置 | Cloudflare Pages 的 SPA fallback，所有路由返回 index.html |
| `dist/_headers` | 安全头配置 | CSP、X-Frame-Options 等安全头 |

### 17.3 worker-api.ts 架构说明

`worker-api.ts` 是从 `server/index.ts`（Express 版本）转换而来的 Cloudflare Workers 版本，使用 Hono 框架。

**核心对应关系**：

| Express 版本 | Workers 版本（worker-api.ts） |
|---|---|
| `express()` | `new Hono()` |
| `app.get/post/put/...` | `app.get/post/put/...`（Hono API 几乎相同） |
| `db.prepare(sql).all(...args)` | `await all(sql, ...args)`（封装函数） |
| `db.prepare(sql).get(...args)` | `await get(sql, ...args)`（封装函数） |
| `db.prepare(sql).run(...args)` | `await run(sql, ...args)`（封装函数） |
| `res.json(data)` | `c.json(data)` |
| `req.body` | `await c.req.json()` |
| `req.params.id` | `c.req.param('id')` |
| `req.header('authorization')` | `c.req.header('authorization')` |
| `process.env.JWT_SECRET` | `c.env.JWT_SECRET`（通过 wrangler.toml [vars] 配置） |

**D1 数据库操作封装**（worker-api.ts L42-L53）：
```typescript
export async function all<T>(sql: string, ...args: any[]): Promise<T[]> {
  const r = await D1.prepare(sql).bind(...args).all()
  return r.results as T[]
}
export async function get<T>(sql: string, ...args: any[]): Promise<T | undefined> {
  const r = await D1.prepare(sql).bind(...args).first()
  return r as T | undefined
}
export async function run(sql: string, ...args: any[]): Promise<{ lastInsertRowid: number | bigint }> {
  const r = await D1.prepare(sql).bind(...args).run()
  return { lastInsertRowid: r.meta.last_row_id as number | bigint }
}
```

**CORS 中间件**（动态 origin echo）：
```typescript
app.use('*', async (c, next) => {
  const origin = c.req.header('Origin') || '*'
  c.header('Access-Control-Allow-Origin', origin)
  c.header('Access-Control-Allow-Methods', 'GET, POST, PUT, PATCH, DELETE, OPTIONS')
  c.header('Access-Control-Allow-Headers', 'Content-Type, Authorization')
  c.header('Access-Control-Allow-Credentials', 'true')
  if (c.req.method === 'OPTIONS') return c.text('', 204)
  await next()
})
```

### 17.4 部署命令速查

```bash
# 部署后端 Workers
cd /workspace
npx wrangler deploy

# 部署前端 Pages（手动，通常 Git push 自动触发）
npx wrangler pages deploy dist --project-name=zhuiguang-pages

# D1 数据库操作
npx wrangler d1 execute zhuiguang-db --remote --command="SELECT COUNT(*) FROM users"
npx wrangler d1 execute zhuiguang-db --remote --file=schema.sql

# 本地开发（使用本地 D1）
npx wrangler dev

# 查看 Workers 日志
npx wrangler tail
```

### 17.5 环境变量配置

环境变量在 `wrangler.toml` 的 `[vars]` 段配置，或通过 Cloudflare Dashboard → Workers → Settings → Variables 配置：

| 变量名 | 说明 | 当前值位置 |
|---|---|---|
| `DB` | D1 数据库 binding（不是变量，是 binding） | wrangler.toml `[[d1_databases]]` |
| `JWT_SECRET` | JWT 签名密钥 | wrangler.toml `[vars]` |
| `JWT_EXPIRES` | JWT 过期时间 | wrangler.toml `[vars]`（7d） |
| `SUPABASE_URL` | Supabase 项目 URL | wrangler.toml `[vars]` |
| `SUPABASE_SERVICE_KEY` | Supabase service_role 密钥 | wrangler.toml `[vars]` |
| `SUPABASE_BUCKET` | Supabase 存储桶名 | wrangler.toml `[vars]`（zhuiguang） |

前端环境变量在 Cloudflare Pages 项目设置中配置：
| 变量名 | 说明 | 示例值 |
|---|---|---|
| `VITE_API_BASE_URL` | 后端 API 地址 | `https://api.xkzg.dpdns.org` 或 `https://zhuiguang-api.xxx.workers.dev` |

### 17.6 已知限制与注意事项

1. **Workers CPU 限制**：免费套餐 10ms CPU/请求。监控接口 `/api/admin/monitor` 执行 20+ 条 SQL 查询，可能接近限制。如遇超时，考虑拆分查询或添加缓存。
2. **D1 写入限制**：免费套餐 100,000 行写/天。当前用户量（70人）远不会触及。
3. **Workers 子请求限制**：免费套餐 50 子请求/请求。监控接口的循环查询需注意。
4. **D1 不支持的部分 SQLite 功能**：`PRAGMA journal_mode`、`VACUUM` 等本地 PRAGMA 不可用。
5. **`Number()` 转换**：D1 返回的 `last_row_id` 是 number 类型（不是 bigint），但代码中仍保留 `Number()` 转换以防万一。
6. **Cold Start**：Workers 冷启动约 0-5ms，用户基本无感知。
7. **CSS 代码分割**：Vite 构建时必须关闭 CSS code splitting 和 module preloading（`cssCodeSplit: false, modulePreload: false`），否则 Cloudflare Pages 加载 CSS 会失败。

### 17.7 迁移过程中修复的问题

| 问题 | 根因 | 修复方法 |
|---|---|---|
| CORS 跨域错误 | 前端 xkzg.dpdns.org 和后端 api.xkzg.dpdns.org 不同源 | worker-api.ts 动态 echo Origin + Allow-Credentials |
| 浏览器缓存旧前端 | 迁移后浏览器仍缓存旧域名资源 | 清除 service worker + 版本化 URL |
| 监控页面不兼容 | MonitorView.vue 显示服务器 CPU/内存，Workers 无此概念 | 重写监控页面，显示 Cloudflare 边缘节点、D1 统计 |
| CSS preload 失败 | Vite 默认 CSS code splitting 在 Pages 上不工作 | vite.config.ts 设置 `cssCodeSplit: false, modulePreload: false` |
| 前端 API 地址硬编码 | 旧代码 API 地址写死 localhost:3001 | 改用 `VITE_API_BASE_URL` 环境变量动态注入 |

---

## 18. 管理后台运行监控（Cloudflare适配版）

### 18.1 监控页面位置

文件：`src/views/admin/MonitorView.vue`
路由：`/admin/monitor`
权限：仅 SUPER_ADMIN 可访问

### 18.2 监控数据接口

接口：`GET /api/admin/monitor`（worker-api.ts 中实现）
返回数据结构：

```json
{
  "online": {
    "online5min": 3,        // 5分钟内活跃用户
    "online1hour": 8,       // 1小时内活跃用户
    "totalUsers": 65,       // 总用户数
    "activeUsers": 60,      // 活跃用户数（status=active）
    "todayLogins": 12,      // 今日登录数
    "todayArticles": 3,     // 今日美文数
    "todayResources": 1,    // 今日资料数
    "todayExps": 45         // 今日经验值变动
  },
  "database": {
    "fileSize": 524288,     // D1数据库大小（字节）
    "fileSizeFmt": "512.0 KB",
    "tables": {              // 各表行数
      "users": 65,
      "articles": 120,
      "resources": 45,
      ...
    }
  },
  "platform": {
    "runtime": "Cloudflare Workers",
    "colo": "SJC",          // 边缘节点（San Jose）
    "country": "US",
    "httpProtocol": "HTTP/2",
    "tlsVersion": "TLSv1.3",
    "isEdge": true,
    "d1Region": "WNAM",     // D1数据库区域（West North America）
    "d1SizeLimit": "500 MB (免费套餐)",
    "workerCpuLimit": "10ms CPU/请求 (免费套餐)",
    "workerSubrequests": "50 子请求/请求"
  },
  "subjectDist": [          // 学科内容分布（饼图）
    { "name": "📖 语文", "value": 25 },
    { "name": "📐 数学", "value": 30 },
    ...
  ],
  "roleDist": [             // 用户角色分布
    { "name": "学生", "value": 58 },
    { "name": "教师", "value": 6 },
    { "name": "超级管理员", "value": 1 }
  ],
  "pending": {
    "articles": 2,          // 待审核美文
    "resources": 1          // 待审核资料
  },
  "dailyActive": [          // 7天活跃趋势（柱状图+折线图）
    { "date": "08-02", "users": 15, "articles": 5 },
    ...
  ]
}
```

### 18.3 图表渲染机制

监控页面使用 ECharts 渲染两张图表：
1. **7天活跃趋势图**（柱状图+折线图）：活跃用户数（柱）+ 美文数（线）
2. **学科内容分布饼图**（环形图）：各学科的美文+资料总数

图表渲染采用5重保险机制（防止"需要手动点刷新才显示"的问题）：
1. `watch([data, chart1, chart2], {flush:'post'})` 监听数据+DOM就绪
2. `nextTick()` + `requestAnimationFrame(resize)` 确保DOM渲染完成
3. `scheduleRender(attempt:1~3)` 每300ms重试，最多3次
4. DOM 兜底：`document.querySelector('[data-chart="c1"]')` 即使 Vue ref 丢失也能找到容器
5. `setOption(option, true)` 强制不合并 + `dispose()` 重建防止脏状态

### 18.4 【历史】旧版监控页面（已废弃）

旧版 MonitorView.vue 显示服务器 CPU 使用率、内存占用、进程数等本地服务器指标。迁移到 Cloudflare 后这些指标不再有意义，已完全重写。

---

## 19. 全局索引表（自定义名词/文件/模块速查）

### 19.1 项目专属术语

| 术语 | 释义 |
|---|---|
| 追光 | 项目名，取"追寻光芒"之意 |
| 美文 | 学生/教师发布的学科相关文章，支持 Markdown 富文本 |
| 资料 | 上传的学科资源文件（PDF/PPT/Word/Excel/视频/图片等） |
| 单题训练 | 学科题目池中逐题练习的模式 |
| 题库自测 | 教师创建的限时测验，支持单选/多选/判断/填空 |
| 数据查询 | 教师导入 Excel 成绩表，学生按姓名/学号查自己的成绩 |
| 经验值 | 用户行为积分系统，登录/发文/上传/做题等行为获得积分 |
| pending_student | 美文特殊状态：教师代学生发文后，等学生确认才进入审核流程 |
| presign | Supabase 预签名上传 URL，前端直传文件到 Supabase Storage |
| start-all.sh | 【已废弃】旧架构的全量启动脚本 |
| update.sh | 【已废弃】旧架构的一键更新脚本 |
| tunnel-keeper | 【已废弃】旧架构的隧道守护脚本 |
| never-die-guard | 【已废弃】旧架构的不死守护进程 |
| worker.js | 【已废弃】旧架构的 Cloudflare Worker 反向代理代码 |
| worker-api.ts | 【当前】Cloudflare Workers 后端 API（Hono 框架） |
| wrangler.toml | 【当前】Cloudflare Workers 部署配置 |
| D1 | Cloudflare 的 SQLite 兼容无服务器数据库 |
| Pages | Cloudflare Pages 静态网站托管服务 |

### 19.2 核心文件速查

| 文件路径 | 用途 | 架构状态 |
|---|---|---|
| `worker-api.ts` | Workers 后端 API | 当前生产 |
| `wrangler.toml` | Workers 部署配置 | 当前生产 |
| `schema.sql` | D1 建表脚本 | 当前生产 |
| `scripts/migrate-to-d1.sh` | 数据迁移脚本 | 迁移工具 |
| `src/api/http.ts` | 前端 API 地址配置 | 当前生产 |
| `src/api/index.ts` | 前端 API 方法封装 | 当前生产 |
| `src/views/admin/MonitorView.vue` | 监控仪表盘 | 当前生产（已重写） |
| `server/index.ts` | Express 后端（本地开发用） | 历史/本地开发 |
| `server/db.ts` | 本地 SQLite 连接 | 历史/本地开发 |
| `server/auth.ts` | 鉴权中间件 | 历史/本地开发 |
| `server/helpers.ts` | 业务辅助函数 | 历史/本地开发 |
| `server/storage.ts` | Supabase 存储封装 | 历史/本地开发 |
| `worker.js` | 旧 Worker 反向代理 | 已废弃 |
| `start-all.sh` | 旧全量启动脚本 | 已废弃 |
| `update.sh` | 旧一键更新脚本 | 已废弃 |
| `tunnel-keeper.sh` | 旧隧道守护 | 已废弃 |
| `never-die-guard.sh` | 旧不死守护 | 已废弃 |
| `keep-alive.sh` | 旧保活脚本 | 已废弃 |
| `vite.config.ts` | Vite 构建配置 | 当前生产 |
| `.env` | 本地环境变量 | 不入库 |
| `wrangler.toml` | Workers 配置（含环境变量） | 入库（密钥在[vars]中） |

### 19.3 数据库表速查

| 表名 | 用途 | 关键字段 |
|---|---|---|
| `users` | 用户 | id, username, role, exp, level, status, subject_id, last_active |
| `classes` | 班级 | id, name, grade |
| `class_members` | 班级成员 | class_id, user_id, role_in_class, subject_id |
| `subjects` | 学科 | id, name, slug, icon, color, modules, display_order |
| `articles` | 美文 | id, title, content, subject_id, user_id, actual_user_id, status |
| `resources` | 资料资源 | id, subject_id, title, file_path, file_type, file_size, status |
| `exp_logs` | 经验值流水 | user_id, action_type, exp_change, description |
| `notices` | 站内通知 | user_id, title, content, type, read |
| `messages` | 私信 | from_id, to_id, content, attachments, is_read |
| `quizzes` | 测验 | subject_id, title, time_limit, status |
| `quiz_questions` | 测验题目 | quiz_id, type, content, options, answer |
| `quiz_submissions` | 测验提交 | quiz_id, user_id, score, answers |
| `subject_questions` | 单题训练题库 | subject_id, qtype, content, answer, options, score, attachments |
| `practice_submissions` | 练习提交记录 | question_id, user_id, answer, score, max_score, status, correct, comment, submitted_at, graded_at |
| `query_tasks` | 成绩查询任务 | subject_id, class_id, creator_id, title, headers, match_field |
| `query_rows` | 成绩查询数据行 | task_id, match_value, data (JSON) |
| `pages` | 通用页面（博客/公告/说明） | slug, title, content, type |
| `page_comments` | 页面评论 | page_id, user_id, content |
| `likes_map` | 点赞/收藏 | user_id, target_type, target_id（唯一键） |
| `article_comments` | 美文评论 | article_id, user_id, user_name, content |
| `feature_flags` | 功能开关KV | key, value ('0'/'1') |
| `settings` | 全局设置 | key, value (JSON) |
| `themes` | 主题 | name, config (JSON) |

---

## 附录A：文档更新记录（必须维护，每次更新文档都要追加一行）

| 日期 | 文档版本 | 更新人 | 修改章节 | 修改摘要 | 需要重新部署 | 备注 |
|---|---|---|---|---|---|---|
| 2026-08-10 | v2.1 | 开发助手 | 第6章核心文件索引、第11章功能清单、第16章时间线、附录A | 单题训练按题维度统计和记录管理：新增后端API GET/DELETE /api/practice/stats/:questionId、GET/DELETE /api/practice/record/:id、GET /api/practice/my-records；新增前端页面 PracticeRecordsView.vue 和 PracticeStatsView.vue；题库自测tab每道题卡片增加统计按钮 | 是（已执行） | 配套：server/index.ts + worker-api.ts 同步修改；前端 npm run build 成功；npx wrangler deploy 成功 |
| 2026-08-08 | v1.2 → v2.0 | 开发助手 | 全文19章大更新：第1章项目身份卡新增Cloudflare架构字段、第3章新增3.0当前生产架构+3.3标注历史+3.4文件部署对应关系、第4章新增4.0当前生产技术栈、第5章新增D1数据库说明、第6章新增worker-api.ts等文件索引+代码发布流程v2.0、第7章上传架构补充Cloudflare说明、第16章时间线追加8条迁移事件、新增第17章Cloudflare Workers+D1迁移架构、新增第18章管理后台运行监控、新增第19章全局索引表、附录B检查清单更新 | Cloudflare Workers+D1+Pages迁移完成：worker-api.ts(Hono框架115+路由)+wrangler.toml+schema.sql+migrate-to-d1.sh；创建GitHub全套配套文件(README/CONTRIBUTING/CHANGELOG/FAQ/LICENSE)；更新AI启动提示词；监控仪表盘适配无服务器环境 | 是 | 配套：GitHub docs已创建；AI提示词已更新；三类产出信息一致性已交叉校验 |

> 规则：
> 1. 每次修改本文档（无论大小）都在下表追加一行；
> 2. 「更新人」填写：真实姓名 / AI开发助手 / GitHub用户名；
> 3. 「修改章节」填具体修改了哪一章节，不要只写"全文"；
> 4. 「是否需要重新部署」：如果只是文档文字改动=否；如果修改了文中描述的业务逻辑（比如新表、新字段、新Bug清单但代码也改了）= 是，并备注是否已执行 `bash update.sh`。

| 日期 | 文档版本 | 更新人 | 修改章节 | 修改摘要 | 需要重新部署 | 备注 |
|---|---|---|---|---|---|---|
| 2026-08-04 上午 | v1.0 | 开发助手 | 全文1-16章 | 初版完整交接文档撰写完成 | 否 | 配套代码版本：同日上午推送main分支 |
| **2026-08-04 下午** | **v1.0 → v1.1** | **开发助手** | **头部版本号、第5章数据库表users/articles/新增article_comments+feature_flags、第10章Bug清单BUG-11~BUG-19、第11章功能清单补8条、第16章时间线追加2条、新增附录A文档更新记录** | **本轮用户报告9条业务Bug修复完成并全部通过API级回归验证，同步更新了数据表结构说明、Bug修复清单条目、功能完成清单、项目时间线** | **是（已执行）** | **配套：已完成 `npm run build` + `git push origin main` + 服务器 `bash update.sh`；配套工作日志：`/workspace/开发工作日志_追光平台.md`** |
| **2026-08-05 凌晨** | **v1.1 → v1.2** | **开发助手** | **第10章Bug清单新增BUG-20、第11章功能清单补「Worker v5双路兜底/start-all依赖加固/全免费不绑卡合规」、第16章时间线追加2条「BUG-20修复/流程规范升级」、附录A追加本次更新行、第12.5节Worker部署步骤加强调提示** | **BUG-20修复：1016重连卡死几十分钟→start-all依赖16包检测+Worker v5双路兜底(Raw+API绕过CDN)+应急换URL清零failCount；流程规范升级=全免费不绑卡+联网引用必须真实URL；Secret Scanning拦截Token明文已修复脱敏** | **是（已执行4次小commit分批push，避免GitHub仓库规则拒绝）** | **配套：4 commits push main：600f71f(worker+start-all) → d4fe6e0(删废弃Dockerfile/cloudflared) → 30f09ae(工作日志+交接v1.2，Token脱敏)；服务器bash update.sh执行完成；线上连续15轮HTTP200验证通过** |
| **2026-08-08** | **v1.2 → v2.0** | **开发助手** | **全文19章大更新：第1章项目身份卡新增Cloudflare架构字段、第3章新增3.0当前生产架构+3.3标注历史+3.4文件部署对应关系、第4章新增4.0当前生产技术栈、第5章新增D1数据库说明、第6章新增worker-api.ts等文件索引+代码发布流程v2.0、第7章上传架构补充Cloudflare说明、第16章时间线追加8条迁移事件、新增第17章Cloudflare Workers+D1迁移架构、新增第18章管理后台运行监控、新增第19章全局索引表、附录B检查清单更新** | **Cloudflare Workers+D1+Pages迁移完成：worker-api.ts(Hono框架115+路由)+wrangler.toml+schema.sql+migrate-to-d1.sh；创建GitHub全套配套文件(README/CONTRIBUTING/CHANGELOG/FAQ/LICENSE)；更新AI启动提示词；监控仪表盘适配无服务器环境** | **是** | **配套：GitHub docs已创建；AI提示词已更新；三类产出信息一致性已交叉校验** |
| **2026-08-10 下午** | **v2.1.1 → v2.1.2** | **开发助手** | **第10章Bug清单新增BUG-23、第11章功能清单补统计接口优化、第16章时间线追加v2.1.2里程碑、附录A追加本次更新行** | **统计模块死代码清理：移除后端无用scoreDist SQL查询和前端scoreDist变量；统计接口返回精简为question/subject/stats/pendingSubs/detailSubs四项** | **是（已执行）** | **配套：worker-api.ts+server/index.ts+src/views/quiz/PracticeStatsView.vue同步修改；npm run build成功；npx wrangler deploy成功(v ea4fe6f5)；git push origin main成功** |
| **2026-08-10 下午** | **v2.1.3 → v2.1.4** | **开发助手** | **第10章Bug清单新增BUG-26、第16章时间线追加v2.1.4里程碑、附录A追加本次更新行** | **修复主观题批改后correct字段为null导致显示红色✗：批改接口补全correct字段计算写入(score>=max_score为1，否则0)** | **是（已执行）** | **配套：worker-api.ts+server/index.ts各修改一行UPDATE；npm run build成功；npx wrangler deploy成功；git push origin main成功** |

---

## 附录B：每次修复后必做检查清单（Checklist）

> 维护者每次改完代码、准备发版前，**必须一条条勾选**，勾完才能部署。

- [ ] 1. 本地 `npm run build` 构建成功（无 Vue-tsc 类型错误）
- [ ] 2. **三个角色**（admin / 测试教师 / 123学生）都能正常登录
- [ ] 3. 执行回归脚本：`python3 /data/user/work/full_verify_9bugs_v2.py` → **必须10/10 PASS**（如脚本仍适用于当前架构）
- [ ] 4. 随机抽查 3 篇美文详情页：
  - [ ] 作者名不是admin（除非是admin自己发的）
  - [ ] 能发评论，评论显示正常，不报错
  - [ ] 状态标签颜色正确（pending黄色、rejected红色、approved绿色/无标签）
- [ ] 5. 教师账号（测试教师）登录 → 左侧菜单**看不到**「用户管理」「功能开关」「经验设置」
- [ ] 6. 功能开关里关掉「自助注册」→ 登录页注册按钮消失/灰；再开启恢复
- [ ] 7. `git status` 只有预期文件被改动，`.env` 和 `server/local.db` 未被修改
- [ ] 8. `git push origin main` 成功（GITHUB_TOKEN有效）
- [ ] 9. 改了后端代码 → `cd /workspace && npx wrangler deploy` 部署成功
- [ ] 10. 改了前端代码 → 确认 Cloudflare Pages 自动构建完成（Dashboard 查看构建状态）
- [ ] 11. 打开 https://xkzg.dpdns.org 实际访问：首屏不白屏、超管登录后看板正常加载
- [ ] 12. 在 `/workspace/开发工作日志_追光平台.md` 追加本次操作记录
- [ ] 13. 在「附录A：文档更新记录」如果改了本文档 → 追加一行

✅ 勾完 13 项 = 合格发版
